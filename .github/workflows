name: Deploy

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Build Gatsby
        run: |
          yarn build

      - name: Deploy to Staging
        run: |
          touch public/CNAME && echo nickdrane.com > public/CNAME