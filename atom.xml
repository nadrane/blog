<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>Nick Drane</title>
  
  <subtitle>Learn Modern Web Development</subtitle>
  <link href="/atom.xml" rel="self"/>
  
  <link href="http://nickdrane.com/"/>
  <updated>2018-10-19T03:30:33.439Z</updated>
  <id>http://nickdrane.com/</id>
  
  <author>
    <name>Nick Drane</name>
    
  </author>
  
  <generator uri="http://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Using jq to Effortlessly Ingest Line-delimited JSON into PostgreSQL</title>
    <link href="http://nickdrane.com/using-jq-to-effortlessly-ingest-newline-delimited-JSON-into-postgres/"/>
    <id>http://nickdrane.com/using-jq-to-effortlessly-ingest-newline-delimited-JSON-into-postgres/</id>
    <published>2018-10-18T05:00:00.000Z</published>
    <updated>2018-10-19T03:30:33.439Z</updated>
    
    <content type="html"><![CDATA[<p>I recently wanted to ingest a <a href="https://en.wikipedia.org/wiki/JSON_streaming#Line-delimited_JSON" target="_blank" rel="noopener">line-delimited</a> JSON file into <a href="https://www.postgresql.org/" target="_blank" rel="noopener">Postgres</a> for some quick data exploration. I was surprised when I couldn’t find a simple solution online. Here is my approach.</p><a id="more"></a><h2 id="Downloading-250000-Hacker-News-Comments"><a href="#Downloading-250000-Hacker-News-Comments" class="headerlink" title="Downloading 250000 Hacker News Comments"></a>Downloading 250000 Hacker News Comments</h2><p>Let’s say we want to download all of the <a href="https://news.ycombinator.com/" target="_blank" rel="noopener">Hacker News</a> comments from the month of May. A line-delimited JSON file is available from <a href="https://files.pushshift.io/hackernews/HNI_2018-05.bz2" target="_blank" rel="noopener">pushshift</a>. Fetching and decompressing the file is simple:</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">curl https://files.pushshift.io/hackernews/HNI_2018-05.bz2 | bzip2 -d</span><br></pre></td></tr></table></figure><p>Here is what the dataset looks like:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  <span class="attr">"by"</span>: <span class="string">"criddell"</span>,</span><br><span class="line">  <span class="attr">"id"</span>: <span class="number">16966059</span>,</span><br><span class="line">  <span class="attr">"kids"</span>: [</span><br><span class="line">    <span class="number">16966312</span>,</span><br><span class="line">    <span class="number">16966776</span>,</span><br><span class="line">    <span class="number">16969455</span>,</span><br><span class="line">    <span class="number">16966323</span></span><br><span class="line">  ],</span><br><span class="line">  <span class="attr">"parent"</span>: <span class="number">16965363</span>,</span><br><span class="line">  <span class="attr">"retrieved_on"</span>: <span class="number">1528401399</span>,</span><br><span class="line">  <span class="attr">"text"</span>: <span class="string">"Yeah - there's always a HATEOAS comment somewhere and..."</span>,</span><br><span class="line">  <span class="attr">"time"</span>: <span class="number">1525173078</span>,</span><br><span class="line">  <span class="attr">"type"</span>: <span class="string">"comment"</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Formatting-the-Data"><a href="#Formatting-the-Data" class="headerlink" title="Formatting the Data"></a>Formatting the Data</h2><p>You might think that Postgres has a simple utility for loading line-delimited JSON. Like me, you’d be wrong. It’s all the more surprising given that it has a <a href="https://www.postgresql.org/docs/current/static/sql-copy.html" target="_blank" rel="noopener">COPY</a> utility that’s designed to load data from files. Unfortunately, that utility only supports <code>text</code>, <code>csv</code>, and <code>binary</code> formats.</p><p>Transforming our data into a CSV is a breeze with <a href="https://stedolan.github.io/jq/" target="_blank" rel="noopener">jq</a>. We can pipe the JSON stream into the following command to extract the <code>id</code>, <code>by</code>, <code>parent</code>, and <code>text</code> fields. You can customize the command to extract whatever fields you like.</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">jq -r <span class="string">'[.id, .by, .parent, .text] | @csv'</span></span><br></pre></td></tr></table></figure><p>The <code>-r</code> option indicates that we would like a raw string output, as opposed to JSON formatted with quotes. The <code>[.id, .by, .parent, .text]</code> part produces an array containing the desired fields and the pipe into <code>@csv</code> specifies the format. All that’s left is to load the data into Postgres.</p><h2 id="Ingesting-the-Data"><a href="#Ingesting-the-Data" class="headerlink" title="Ingesting the Data"></a>Ingesting the Data</h2><p>After creating the database</p><p><code>createdb comment_db</code></p><p>and applying the schema</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">CREATE</span> <span class="keyword">TABLE</span> <span class="keyword">comment</span> (</span><br><span class="line">    <span class="keyword">id</span> <span class="built_in">INTEGER</span> PRIMARY <span class="keyword">KEY</span>,</span><br><span class="line">    <span class="keyword">by</span> <span class="built_in">VARCHAR</span>,</span><br><span class="line">    <span class="keyword">parent</span> <span class="built_in">INTEGER</span>,</span><br><span class="line">    <span class="built_in">text</span> <span class="built_in">TEXT</span></span><br><span class="line">);</span><br></pre></td></tr></table></figure><p>we can hydrate our comments into <code>comment_db</code> using <a href="https://www.postgresql.org/docs/current/static/app-psql.html" target="_blank" rel="noopener">psql</a></p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">psql comment_db -c <span class="string">"COPY comment (id, by, parent, text) FROM STDIN WITH (FORMAT CSV)"</span></span><br></pre></td></tr></table></figure><p>Note that the fields specified above need to be in the same order as the fields in the CSV stream generated by <code>jq</code>.</p><p>Here is the final command</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">curl https://files.pushshift.io/hackernews/HNI_2018-05.bz2 \</span><br><span class="line">  | bzip2 -d \</span><br><span class="line">  | jq -r <span class="string">'[.id, .by, .parent, .text] | @csv'</span> \</span><br><span class="line">  | psql comment_db -c <span class="string">"COPY comment (id, by, parent, text) FROM STDIN WITH (FORMAT CSV)"</span></span><br></pre></td></tr></table></figure><p>One final caveat. You might notice that even though the <code>parent</code> column references a comment id, I’ve neglected to specify the foreign key constraint. This is because our command does not control for the order that comments are loaded. We would get a constraint error if a comment references a parent comment that has not yet been inserted. It would probably be easiest to apply these constraints after ingesting the data.</p><p>My name is Nick Drane. I do <a href="/hire-me">consulting</a> out of Chicago area and am looking for new opportunities.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;I recently wanted to ingest a &lt;a href=&quot;https://en.wikipedia.org/wiki/JSON_streaming#Line-delimited_JSON&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;line-delimited&lt;/a&gt; JSON file into &lt;a href=&quot;https://www.postgresql.org/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Postgres&lt;/a&gt; for some quick data exploration. I was surprised when I couldn’t find a simple solution online. Here is my approach.&lt;/p&gt;
    
    </summary>
    
      <category term="Postgres" scheme="http://nickdrane.com/categories/Postgres/"/>
    
      <category term="Command Line" scheme="http://nickdrane.com/categories/Command-Line/"/>
    
    
  </entry>
  
  <entry>
    <title>The Hidden Costs of PostgreSQL&#39;s JSONB Datatype</title>
    <link href="http://nickdrane.com/hidden-costs-of-postgresql-jsonb/"/>
    <id>http://nickdrane.com/hidden-costs-of-postgresql-jsonb/</id>
    <published>2018-09-30T05:00:00.000Z</published>
    <updated>2018-10-19T03:26:37.843Z</updated>
    
    <content type="html"><![CDATA[<p><a href="https://www.postgresql.org/" target="_blank" rel="noopener">Postgres</a> introduced the <a href="https://www.postgresql.org/docs/current/static/datatype-json.html" target="_blank" rel="noopener">JSONB</a> type in version 9.4 with considerable excitement. JSONB promised to marry a favorite relational database with the noSQL world, permitting data to be stored in the database as JSON without the need for re-parsing whenever a field is accessed. Moreover, the binary storage format permits indexing and complex queries against the stored JSON blobs. This data format embodies the flexible schema and was readily adopted at <a href="https://fraight.ai/" target="_blank" rel="noopener">Fraight</a>.</p><a id="more"></a><h2 id="Background"><a href="#Background" class="headerlink" title="Background"></a>Background</h2><p>At Fraight, we’ve built a centralized communication platform that collates all inbound/outbound communications between our brokerage company and thousands of trucking partners. One of our main objectives is to build a system that parses and automatically responds to inbound text messages, emails, and faxes. We knew we would eventually need the nitty-gritty details of these messages, so we captured the data by dumping entire http response bodies into a JSONB column named <code>meta</code> in our database’s <code>message</code> table.</p><p>In an ideal world, the third party API responses we collected would have been broken down into discrete chunks and stored in separate columns, but our approach was a pragmatic design decision. We knew it wasn’t worth the engineering effort to try to understand the multitude of fields we receive from a half-dozen APIs, particularly when we had no idea at the time how we might use this information. But we did know it was valuable. And since we wanted it to be queryable, we chose JSONB over it’s more inert and sometimes more size efficient cousin, the JSON datatype.</p><h2 id="Discovery"><a href="#Discovery" class="headerlink" title="Discovery"></a>Discovery</h2><p>Fraight’s CTO approached me the other day and explained that query performance over the <code>message</code> table had deteriorated. He elaborated that the queries were only slow when the <code>meta</code> column was included in the result set. We had previously experienced slowdown in the <code>message</code> table when entire email attachment bodies were getting serialized and stored in the <code>meta</code> column, and I suspected that the root cause of our current performance problem was in a similar vein. A quick <a href="#footnote1">query</a> revealed that our <code>meta</code> column was often quite large.</p><p>The average size of the <code>meta</code> column was 3.7 kb. That might not seem large, but for our 100,000 row table, that meant 400mb of (mostly unused) metadata. At the high end of the spectrum, some messages were up to 17mb in size. The precise details of why this dataset slows down queries are a bit esoteric<sup><a href="#footnote2">2</a></sup>, but it was clear that we were storing too much information. You can read more about how Postgres stores large data values using <a href="https://www.postgresql.org/docs/current/static/storage-toast.html" target="_blank" rel="noopener">TOAST</a> and <a href="#footnote3">how I validated this was a problem</a>.</p><h2 id="Further-Complications"><a href="#Further-Complications" class="headerlink" title="Further Complications"></a>Further Complications</h2><p>The first queries against the <code>meta</code> column were for very simple tasks like showing the raw contents of an email to a user, perhaps in the case where message content extraction failed. Over time we began using specific fields to drive business logic, and our application code started to expect that <code>meta</code>‘s JSON would adhere to a specific shape. Any proposed solution would likely necessitate changes to this application code.</p><p>So the problem was twofold:</p><ol><li><p>We needed to extract the actual metadata (as opposed to fields that drove business logic) from the <code>meta</code> column and place it in a location where it would not affect query performance over the <code>message</code> table.</p></li><li><p>We wanted to make the metadata less easily accessible. We recognized that making it accessible through the ORM made it ripe for misuse. We wanted to give it an alternative API that would lessen a developer’s likeliness to rely on its structure.</p></li></ol><h2 id="Solutioning"><a href="#Solutioning" class="headerlink" title="Solutioning"></a>Solutioning</h2><p>We looked at several solutions. Below are the three we considered most seriously:</p><ol><li><p>Use the ORM to omit the <code>meta</code> column from all queries unless specifically included. Since we unnecessarily perform <code>select *</code> <sup><a href="#footnote4">4</a></sup> queries over the <code>message</code> table, this strategy, despite its messiness, would increase the performance in most cases (with the occasional slow query when metadata was actually required) and would in theory be simple to implement. It wouldn’t, however, resolve our initial design shortcut.</p></li><li><p>Keep the <code>meta</code> column as-is but extract specific keys to <a href="https://aws.amazon.com/s3/" target="_blank" rel="noopener">S3</a>. In general, greater than 99% of the size of any given column’s <code>meta</code> field was from a single key. For example, many of the emails we capture include large attachments. Other times message bodies retain long, historical email chains. By extracting these problematic fields and uploading them to S3, the database would only need to store a reference to the S3 content. Then, upon request, the server could generate a <a href="https://docs.aws.amazon.com/AmazonS3/latest/dev//ShareObjectPreSignedURL.html" target="_blank" rel="noopener">pre-signed URL</a>, allowing the client to download large files directly from S3.</p></li><li><p>Create a separate <code>metadata</code> table in Postgres. It would have a foreign key back to the original table where the metadata belonged. This solution solves the <code>select *</code> problem described above and offers an additional advantage: since the <code>meta</code> column pattern exists on more than just the <code>message</code> table, it offers a unified strategy for storing metadata.</p></li></ol><p>Our instinct was to go with #1 — the most simple and straightforward solution — and omit the <code>meta</code> column from most queries. Unfortunately, bugs in our ORM made it impossible to omit columns across joins without the occasional crash. We needed an alternate approach.</p><p>This hiccup left the choice between S3 and a separate Postgres table. We agreed that pre-signed S3 URLs offered an ideal long-term alternative but ultimately chose a separate Postgres table for the same reason that we wanted to choose option #1: minimal risk and complexity. Our team is exceptionally experienced with Postgres, and we knew we could hit the ground running. S3, in contrast, had more unknowns, and given how rarely we access most of this metadata today, the value it would add over Postgres was tenuous at best.</p><p>In addition to migrating metadata to its own table, we took a couple additional steps:</p><ol><li><p>We extracted the handful of regularly used fields from the metadata and migrated them into their own columns in the <code>message</code> table. This meant that we didn’t need to retrieve large, megabyte sized blobs whenever we wanted a single field<sup><a href="#footnote5">5</a></sup>. As an added advantage, we regained simple access to database <a href="https://www.postgresql.org/docs/current/static/ddl-constraints.html" target="_blank" rel="noopener">constraints</a>.</p></li><li><p>When we created the new <code>metadata</code> table in Postgres, we made sure not to define a relationship between it and its related tables at the ORM layer, only the database layer. This makes it far more difficult for a developer to hobble performance by absentmindedly joining large metadata into queries. We introduced an API for accessing the metadata instead. The added advantage of this API is that we can now change the underlying implementation to use S3 (or anything else) in the future, without modifying dependent application code.</p></li></ol><p>My name is Nick Drane. I do <a href="/hire-me">consulting</a> work in the Chicago area and am always looking for new opportunities.</p><h4 id="Footnotes"><a href="#Footnotes" class="headerlink" title="Footnotes"></a>Footnotes</h4><p><a name="footnote1">1</a>: I used the following query:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> <span class="keyword">avg</span>(<span class="keyword">octet_length</span>(m.<span class="string">"meta"</span>::<span class="built_in">text</span>))</span><br><span class="line"><span class="keyword">FROM</span> message <span class="keyword">as</span> m</span><br></pre></td></tr></table></figure><p><a name="footnote2">2</a>: I should clarify that performance measurements were done with a local Postgres installation where network congestion/throughput is not a relevant factor.</p><p><a name="footnote3">3</a>:<br>I verified that the <code>meta</code> column was toasting with a little help from the <a href="https://www.postgresql.org/docs/10/static/disk-usage.html" target="_blank" rel="noopener">Postgres docs</a>:</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">SELECT</span> relname, relpages, relpages * <span class="number">8191</span> / (<span class="number">1024</span> * <span class="number">1024</span>) <span class="keyword">as</span> <span class="keyword">size</span></span><br><span class="line"><span class="keyword">FROM</span> pg_class,</span><br><span class="line">     (<span class="keyword">SELECT</span> reltoastrelid</span><br><span class="line">      <span class="keyword">FROM</span> pg_class</span><br><span class="line">      <span class="keyword">WHERE</span> relname = <span class="string">'message'</span>) <span class="keyword">AS</span> ss</span><br><span class="line"><span class="keyword">WHERE</span> <span class="keyword">oid</span> = ss.reltoastrelid <span class="keyword">OR</span></span><br><span class="line">      <span class="keyword">oid</span> = (<span class="keyword">SELECT</span> indexrelid</span><br><span class="line">             <span class="keyword">FROM</span> pg_index</span><br><span class="line">             <span class="keyword">WHERE</span> indrelid = ss.reltoastrelid)</span><br></pre></td></tr></table></figure><p>For specifically the <code>message</code> table, this query returns the number of disk pages in the toast table and their total size in megabytes.</p><table><thead><tr><th>table</th><th>disk pages</th><th>size (mb)</th></tr></thead><tbody><tr><td>toast    table</td><td>17731</td><td>139</td></tr><tr><td>toast index</td><td>209</td><td>1</td></tr></tbody></table><p><a name="footnote4">4</a>: We were not literally doing any <code>select *</code> queries. Our ORM’s message model did, however, specify all of the columns of the <code>message</code> table. In retrospect, this was probably our biggest mistake. It meant that a call to <code>Message.find</code>, which is used all over the place, retrieved all columns on the <code>message</code> table, unless specifically directed otherwise. Usually, for a RESTful API, this is an acceptable performance tradeoff, but it didn’t hold true in this circumstance.</p><p><a name="footnote5">5</a>: As I write this, I wonder if it wouldn’t have been possible to leverage indexes to only retrieve specific pieces of the <code>meta</code> field. I wonder if our ORM provides any support for firstclass fields that are subfields of another field.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;a href=&quot;https://www.postgresql.org/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Postgres&lt;/a&gt; introduced the &lt;a href=&quot;https://www.postgresql.org/docs/current/static/datatype-json.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;JSONB&lt;/a&gt; type in version 9.4 with considerable excitement. JSONB promised to marry a favorite relational database with the noSQL world, permitting data to be stored in the database as JSON without the need for re-parsing whenever a field is accessed. Moreover, the binary storage format permits indexing and complex queries against the stored JSON blobs. This data format embodies the flexible schema and was readily adopted at &lt;a href=&quot;https://fraight.ai/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Fraight&lt;/a&gt;.&lt;/p&gt;
    
    </summary>
    
      <category term="Postgres" scheme="http://nickdrane.com/categories/Postgres/"/>
    
      <category term="Architecture" scheme="http://nickdrane.com/categories/Architecture/"/>
    
    
  </entry>
  
  <entry>
    <title>Ethical Engineering for the Average Engineer</title>
    <link href="http://nickdrane.com/ethical-engineering-for-the-average-engineer/"/>
    <id>http://nickdrane.com/ethical-engineering-for-the-average-engineer/</id>
    <published>2018-09-16T20:41:16.000Z</published>
    <updated>2018-09-30T20:52:27.477Z</updated>
    
    <content type="html"><![CDATA[<p>Two months ago I purchased a GPS device and associated service plan from <a href="https://www.findmespot.com/en/" target="_blank" rel="noopener">SPOT</a>. Today, upon trying to cancel the service, the customer service representative informed me that I had accidentally enrolled myself into a 1 year, $250 contract and that I was unable to cancel. He told me that if I blocked the monthly charges against my credit card that they would report the debt to a collections agency. I was initially upset but soon realized it was a great opportunity to talk about ethics in software engineering.</p><a id="more"></a><h2 id="SPOT’s-Marketing-Materials"><a href="#SPOT’s-Marketing-Materials" class="headerlink" title="SPOT’s Marketing Materials"></a>SPOT’s Marketing Materials</h2><p>The representative told me to look at their website where he claimed it would be very clear that I was signing up for a contract service. When I Googled “spot service plan”, the first link I clicked on directed me to these marketing materials:</p><p><img src="/images/spot-marketing-material.png" alt="Spot Marketing Website"></p><p>If you look at the image above, it appears as if several deliberate decisions have been made to disguise the total cost of the month-to-month arrangement.</p><ol><li>The font of the month-to-month price is large, bold, and colorful. It’s designed to stand out</li><li>The 12 month contractual stipulation, on the other hand, is written in unnecessarily small font</li><li>The contractual stipulation is low contrast and visually underwhelming, particularly when juxtaposed against the big, bright, and bold “monthly cost” header</li><li>The terms of the contract are visually separated from the price</li></ol><p>None of this could be chalked up to accident, except perhaps point 3. Ultimately, this page’s content is designed to mislead the consumer, hurting them for the business’s gain.<sup><a href="#footnote1">1</a></sup></p><h2 id="Software-Ethics-and-Mainstream-Media"><a href="#Software-Ethics-and-Mainstream-Media" class="headerlink" title="Software Ethics and Mainstream Media"></a>Software Ethics and Mainstream Media</h2><p>The software industry is rife with ethics scandals, and they are rarely cut and dry. Some examples</p><ol><li><a href="https://techcrunch.com/2018/09/07/a-dozen-popular-iphone-apps-caught-quietly-sending-user-locations-to-monetization-firms/" target="_blank" rel="noopener">Dozens of iPhone applications selling user location data</a></li><li><a href="https://en.wikipedia.org/wiki/PRISM_(surveillance_program" target="_blank" rel="noopener">United States surveillance software (PRISMA)</a></li><li><a href="https://money.cnn.com/2018/06/01/technology/google-maven-contract/index.html" target="_blank" rel="noopener">DOD’s Project Maven (drone imaging AI)</a></li></ol><p>Two arguments exist in each of these scenarios. iPhone application engineers might argue that they aren’t hurting consumers by selling their location information. Governments argue that they protect their citizens through surveillance software, even if that’s at the cost of citizen privacy. An engineer of that very software might believe that the code itself is not unethical, only improper use of it. A utilitarian might argue that software that facilitates accurate drone strikes is ethical if the cost of the deaths of its targets is outweighed by its net good. Ultimately, it’s often unclear what constitutes unethical. But it raises a good question: as software engineers, what ethical code should we live by?</p><h2 id="Ethics-for-the-Average-Engineer"><a href="#Ethics-for-the-Average-Engineer" class="headerlink" title="Ethics for the Average Engineer"></a>Ethics for the Average Engineer</h2><p>It’s easy to get consumed in the minutia. The media tends to focus on ethics scandals that have immense gravity, either in the scope of the number of people they affect or in the magnitude with which they affect their targets. But the majority of software engineers aren’t writing software that necessitates answering the above questions. Most of us are writing simple e-commerce applications or iPhone apps – boring software.</p><p>The case study with SPOT is interesting precisely because of how boring it is. It’s true to life, and it serves as a reminder that the average engineer also faces ethical dilemmas. And fortunately for us, these “ethical dilemmas” are sometimes pretty cut and dry, provided we have the awareness to recognize them and the courage to speak up. In the case with SPOT, I think most people would agree that making an effort to mislead the consumer is dishonest and thus unethical.</p><h2 id="The-Takeaway"><a href="#The-Takeaway" class="headerlink" title="The Takeaway"></a>The Takeaway</h2><p>Just because you work on boring software does not mean that it can’t negatively impact your users in an unethical way. The first step towards protecting users is increased developer awareness. As a developer, it’s essential to ask how your software affects your users. If it might hurt them, step back and consider the the greater picture. If you think your code might manipulate or mislead your users, it’s time to speak up.</p><h4 id="Footnotes"><a href="#Footnotes" class="headerlink" title="Footnotes"></a>Footnotes</h4><p><a name="footnote1">1</a>: I think it’s important to be fair here. The marketing material across the site is inconsistent and ranges in the clarity of its term of service. <a href="https://www.findmespot.com/en/index.php?cid=131" target="_blank" rel="noopener">This page</a> makes the 12 month contract abundantly clear. <a href="https://www.findmespot.com/en/index.php?cid=130" target="_blank" rel="noopener">This one</a> falls somewhere in the middle.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;Two months ago I purchased a GPS device and associated service plan from &lt;a href=&quot;https://www.findmespot.com/en/&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;SPOT&lt;/a&gt;. Today, upon trying to cancel the service, the customer service representative informed me that I had accidentally enrolled myself into a 1 year, $250 contract and that I was unable to cancel. He told me that if I blocked the monthly charges against my credit card that they would report the debt to a collections agency. I was initially upset but soon realized it was a great opportunity to talk about ethics in software engineering.&lt;/p&gt;
    
    </summary>
    
      <category term="Software Ethics" scheme="http://nickdrane.com/categories/Software-Ethics/"/>
    
    
  </entry>
  
  <entry>
    <title>Build Your Own Nested Query String Encoder/Decoder</title>
    <link href="http://nickdrane.com/build-your-own-nested-query-string-encoder/"/>
    <id>http://nickdrane.com/build-your-own-nested-query-string-encoder/</id>
    <published>2018-04-13T20:17:00.000Z</published>
    <updated>2018-09-30T21:05:29.859Z</updated>
    
    <content type="html"><![CDATA[<p>The other day at work, one of my colleagues was frustrated that he was unable to encode nested objects in a query string and still maintain a readable URL. I went home that night and coded up a simple solution to this problem, and I thought I’d share it here today. This <a href="https://github.com/nadrane/querystring-encoder" target="_blank" rel="noopener">Github repo</a> contains specs and the solution code.</p><a id="more"></a><h2 id="Motivation"><a href="#Motivation" class="headerlink" title="Motivation"></a>Motivation</h2><p>Today, in the Node.js ecosystem, numerous modules exist to encode query strings, but they generally have one of two flaws:</p><ol><li><p>They do not permit the encoding of nested objects.</p></li><li><p>They can encode nested objects, but they delimit nesting using unsafe URL characters, yielding an operation and a result that look like this <sup><a href="#footnote1">1</a></sup>:</p></li></ol><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">encode(&#123;</span><br><span class="line">  a: &#123; <span class="attr">b</span>: <span class="string">'c'</span> &#125;</span><br><span class="line">&#125;);</span><br><span class="line">&gt;&gt;&gt;<span class="string">`a%5Bb%5D=c`</span></span><br></pre></td></tr></table></figure><h2 id="The-Problem-in-Detail"><a href="#The-Problem-in-Detail" class="headerlink" title="The Problem in Detail"></a>The Problem in Detail</h2><p>Node.js provides a <a href="https://nodejs.org/api/querystring.html" target="_blank" rel="noopener"><code>querystring</code></a> module to encode objects to query strings. The only problem is that conforms to an official specification that doesn’t allow nested objects. Unfortunately, this specification does not allow for enough flexibility when creating a RESTful API.</p><p>For example, suppose the client wants to filter a collection of cars by make and model. The route might look like this:</p><p><code>/api/cars?make=honda&amp;model=civic</code></p><p>This URI makes it reasonably clear that we want to filter cars by their make and model.</p><p>What if we wanted to do something more complicated. What if we wanted to filter cars and order them by price?</p><p><code>/api/cars?order=price&amp;make=honda&amp;model=civic</code></p><p>It’s no longer clear which query parameters describe the ordering and which describe the filter. Ideally, we want the url to look like this:</p><p><code>/api/cars?order=price&amp;filter.make=honda&amp;filter.model=civic</code></p><p>If we were to represent the query string of the above URI as a Javascript object, it would probably look like this:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    order: <span class="string">"price"</span>,</span><br><span class="line">    filter: &#123;</span><br><span class="line">        make: <span class="string">"honda"</span>,</span><br><span class="line">        model: <span class="string">"civic"</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>And then we quickly run into our problem. We need to encode the object above into</p><p><code>order=price&amp;filter.make=honda&amp;filter.model=civic</code></p><p>but Node.js’s <a href="https://nodejs.org/api/querystring.html" target="_blank" rel="noopener"><code>querystring</code></a> can’t encode nested objects.</p><h2 id="Existing-Modules-Supporting-Nested-Querystrings"><a href="#Existing-Modules-Supporting-Nested-Querystrings" class="headerlink" title="Existing Modules Supporting Nested Querystrings"></a>Existing Modules Supporting Nested Querystrings</h2><p>By default, the <a href="https://www.npmjs.com/package/qs" target="_blank" rel="noopener">qs</a> module creates ugly urls when it encodes nested query strings. If we encode our object above, we get</p><p><code>order=price&amp;filter[make]=honda&amp;filter[model]=civic</code></p><p>The <code>[</code> and <code>]</code> characters are both considered unsafe in a URL and are required to be escaped. The URL becomes unreadable after this percent encoding operation.</p><p><code>order=price&amp;filter%5Bmake%5D=honda&amp;filter%5Bmodel%5D=civic</code></p><p>Fortunately, the <code>.</code> is not considered unsafe and does not need to be escaped, making it the perfect character to express object nesting.</p><h2 id="The-Solution"><a href="#The-Solution" class="headerlink" title="The Solution"></a>The Solution</h2><p>The solution is broken down into two parts. The first is <em>encoding</em> a nested object into a query string. The second part is <em>decoding</em> a query string back into a nested object.</p><h3 id="Encoding2"><a href="#Encoding2" class="headerlink" title="Encoding2"></a>Encoding<sup><a href="#footnote2">2</a></sup></h3><h4 id="Just-Nested-Objects"><a href="#Just-Nested-Objects" class="headerlink" title="Just Nested Objects"></a>Just Nested Objects</h4><p>Let’s write some code to encode</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  filter: &#123;</span><br><span class="line">    make: <span class="string">"honda"</span>;</span><br><span class="line">    model: <span class="string">"civic"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>into the query string <code>filter.make=honda&amp;filter.model=civic</code></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> &#123; <span class="built_in">escape</span> &#125; = <span class="built_in">require</span>(<span class="string">"querystring"</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">encode</span>(<span class="params">queryObj, nesting = <span class="string">""</span></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> queryString = <span class="string">""</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> pairs = <span class="built_in">Object</span>.entries(queryObj).map(<span class="function">(<span class="params">[key, val]</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// Handle the nested, recursive case, where the value to encode is an object itself</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="keyword">typeof</span> val === <span class="string">"object"</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> encode(val, nesting + <span class="string">`<span class="subst">$&#123;key&#125;</span>.`</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="comment">// Handle base case, where the value to encode is simply a string.</span></span><br><span class="line">      <span class="keyword">return</span> [nesting + key, val].map(<span class="built_in">escape</span>).join(<span class="string">"="</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="keyword">return</span> pairs.join(<span class="string">"&amp;"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Notice that we use the <a href="https://nodejs.org/api/querystring.html#querystring_querystring_escape_str" target="_blank" rel="noopener">escape</a> function provided in Node.js core to percent encode specific characters.</p><h4 id="Encoding-Arrays-as-Values"><a href="#Encoding-Arrays-as-Values" class="headerlink" title="Encoding Arrays as Values"></a>Encoding Arrays as Values</h4><p>If we want to add support to encode an object with array values, like the following:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">    name: <span class="string">"nick"</span>,</span><br><span class="line">    hobbies: [<span class="string">"cooking"</span>, <span class="string">"coding"</span>]</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>then we only need to add another base case to our function</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">encode</span>(<span class="params">queryObj, nesting = <span class="string">""</span></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> queryString = <span class="string">""</span>;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">const</span> pairs = <span class="built_in">Object</span>.entries(queryObj).map(<span class="function">(<span class="params">[key, val]</span>) =&gt;</span> &#123;</span><br><span class="line">    <span class="comment">// Handle a second base case where the value to encode is an array</span></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">Array</span>.isArray(val)) &#123;</span><br><span class="line">      <span class="keyword">return</span> val.map(<span class="function"><span class="params">subVal</span> =&gt;</span> [nesting + key, subVal].map(<span class="built_in">escape</span>).join(<span class="string">"="</span>)).join(<span class="string">"&amp;"</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> <span class="keyword">if</span> (<span class="keyword">typeof</span> val === <span class="string">"object"</span>) &#123;</span><br><span class="line">      <span class="keyword">return</span> encode(val, nesting + <span class="string">`<span class="subst">$&#123;key&#125;</span>.`</span>);</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> [nesting + key, val].map(<span class="built_in">escape</span>).join(<span class="string">"="</span>);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;);</span><br><span class="line">  <span class="keyword">return</span> pairs.join(<span class="string">"&amp;"</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="Decoding"><a href="#Decoding" class="headerlink" title="Decoding"></a>Decoding</h3><p>An encoding function is not very useful unless you can decode the encoded string back to it’s original form.</p><h4 id="Just-Nested-Objects-1"><a href="#Just-Nested-Objects-1" class="headerlink" title="Just Nested Objects"></a>Just Nested Objects</h4><p>We want to write a function that will decode <code>filter.make=honda&amp;filter.model=civic</code> back into a nested object</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line">&#123;</span><br><span class="line">  filter: &#123;</span><br><span class="line">    make: <span class="string">"honda"</span>;</span><br><span class="line">    model: <span class="string">"civic"</span>;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The code to do this is fairly straightforward if we use a <a href="https://lodash.com/docs" target="_blank" rel="noopener">Lodash</a> utility called <a href="https://lodash.com/docs/4.17.5#set" target="_blank" rel="noopener">set</a> that allows us to set an arbitrarily nested key in an object.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> set = <span class="built_in">require</span>(<span class="string">"lodash.set"</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">decode</span>(<span class="params">queryString</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> queryStringPieces = queryString.split(<span class="string">"&amp;"</span>);</span><br><span class="line">  <span class="keyword">const</span> decodedQueryString = &#123;&#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> piece <span class="keyword">of</span> queryStringPieces) &#123;</span><br><span class="line">    <span class="keyword">let</span> [key, value] = piece.split(<span class="string">"="</span>);</span><br><span class="line">    value = value || <span class="string">""</span>; <span class="comment">// If a value is not defined, it should be decoded as an empty string</span></span><br><span class="line">    set(decodedQueryString, key, value);</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> decodedQueryString;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="Decoding-Arrays-as-Values"><a href="#Decoding-Arrays-as-Values" class="headerlink" title="Decoding Arrays as Values"></a>Decoding Arrays as Values</h4><p>If we want to add support to decode arrays like we did above, then we need to do a little additional work. Fortunately, two additional <a href="https://lodash.com/docs" target="_blank" rel="noopener">Lodash</a> utilities, <a href="https://lodash.com/docs/4.17.5#has" target="_blank" rel="noopener">has</a> and <a href="https://lodash.com/docs/4.17.5#get" target="_blank" rel="noopener">get</a>, allow us to check for the existence of a nested key and to get the value associated with a nested key, respectively, greatly simplifying our problem.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> set = <span class="built_in">require</span>(<span class="string">"lodash.set"</span>);</span><br><span class="line"><span class="keyword">const</span> has = <span class="built_in">require</span>(<span class="string">"lodash.has"</span>);</span><br><span class="line"><span class="keyword">const</span> get = <span class="built_in">require</span>(<span class="string">"lodash.get"</span>);</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">decode</span>(<span class="params">queryString</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> queryStringPieces = queryString.split(<span class="string">"&amp;"</span>);</span><br><span class="line">  <span class="keyword">const</span> decodedQueryString = &#123;&#125;;</span><br><span class="line"></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> piece <span class="keyword">of</span> queryStringPieces) &#123;</span><br><span class="line">    <span class="keyword">let</span> [key, value] = piece.split(<span class="string">"="</span>);</span><br><span class="line">    value = value || <span class="string">""</span>;</span><br><span class="line">    <span class="keyword">if</span> (has(decodedQueryString, key)) &#123;</span><br><span class="line">      <span class="keyword">const</span> currentValueForKey = get(decodedQueryString, key);</span><br><span class="line">      <span class="keyword">if</span> (!<span class="built_in">Array</span>.isArray(currentValueForKey)) &#123;</span><br><span class="line">        set(decodedQueryString, key, [currentValueForKey, value]);</span><br><span class="line">      &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">        currentValueForKey.push(value);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">      set(decodedQueryString, key, value);</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">  <span class="keyword">return</span> decodedQueryString;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>And that’s it! The whole thing, encoding and decoding, only takes ~40 lines of code. Perhaps next time you encounter something that feels a little too fundamental to code yourself, you won’t hesitate to write some code if you can’t find a sufficient open source package.</p><h4 id="Footnotes"><a href="#Footnotes" class="headerlink" title="Footnotes"></a>Footnotes</h4><p><a name="footnote1">1</a>: This example is straight from the <a href="https://www.npmjs.com/package/qs" target="_blank" rel="noopener">qs</a> documentation. Incidentally, qs provides an option to encode using a url safe character, which would result in readable urls, but this is not the default.</p><p><a name="footnote2">2</a>: It’s worth noting that you might not want to use this code in production. I’ve written the code in a functional style for clarity and conciseness. If you have a high read volume, given that this code might potentially run on a significant portion of GET requests, it should probably be written in an imperative style that doesn’t disregard performance. Even more importantly, this code does not protect against potential attackers who might try to create an arbitrarily deeply nested object or might include an unwieldy number of query parameters.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;The other day at work, one of my colleagues was frustrated that he was unable to encode nested objects in a query string and still maintain a readable URL. I went home that night and coded up a simple solution to this problem, and I thought I’d share it here today. This &lt;a href=&quot;https://github.com/nadrane/querystring-encoder&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Github repo&lt;/a&gt; contains specs and the solution code.&lt;/p&gt;
    
    </summary>
    
      <category term="Javascript" scheme="http://nickdrane.com/categories/Javascript/"/>
    
      <category term="Build Your Own" scheme="http://nickdrane.com/categories/Build-Your-Own/"/>
    
      <category term="Recursion" scheme="http://nickdrane.com/categories/Recursion/"/>
    
      <category term="Web" scheme="http://nickdrane.com/categories/Web/"/>
    
    
  </entry>
  
  <entry>
    <title>You&#39;re Hiring Programmers Wrong: A Case for Interview Standardization</title>
    <link href="http://nickdrane.com/you&#39;re-hiring-programmers-wrong-a-case-for-interview-standardization/"/>
    <id>http://nickdrane.com/you&#39;re-hiring-programmers-wrong-a-case-for-interview-standardization/</id>
    <published>2018-03-20T22:03:01.000Z</published>
    <updated>2018-09-30T20:53:11.209Z</updated>
    
    <content type="html"><![CDATA[<p>I’ve conducted about 100 technical interviews over the past 6 months for a software development recruiting company called <a href="https://triplebyte.com/iv/RrzqKKw/bp" target="_blank" rel="noopener">Triplebyte</a>. I’ve also been doing consulting work, which has required me to take numerous technical interviews. It’s interesting contrasting the experiences to identify what works and what doesn’t.</p><p>Every Triplebyte interview begins with the candidate coding a short game. We have a series of steps, and each step precisely defines simple requirements for the program to handle. I can generally tell a couple minutes into the 2 hour interview whether the candidate will be successful. There are certainly outliers (as well as mechanisms to prevent bias), but in general, I can quickly ascertain how well a candidate stacks up technically. So then, it begs the question, why is it so hard to hire engineers? The answer is, most of us are doing it wrong.</p><a id="more"></a><h2 id="Interview-Standardization"><a href="#Interview-Standardization" class="headerlink" title="Interview Standardization"></a>Interview Standardization</h2><p>About 12 months ago, while running <a href="https://www.fullstackacademy.com/" target="_blank" rel="noopener">Fullstack Academy’s</a> Chicago campus, I was looking to hire a qualified instructor. Our interview consisted of live lecture as well as a mock 1-1 teaching session, where the candidate was required to guide me (playing the role of a struggling student) through a simple recursion problem. The latter exercise was almost always a better indicator of instructor fit than the former. The reason was simple: the latter exercise was assigned to the candidate whereas the lecture topic was their choice. This meant that every lecture topic was different and made lectures difficult to compare. In contrast, after having seen dozens of candidates attempt to teach me the same recursion problem, it was obvious who stood out.</p><p>The first mistake that companies make is that they fail to standardize the interview across candidates. As interviewers, it’s tempting to say, “let’s have the candidate pair with us, that way we can see how they perform on real day-to-day challenges.” Or another fault I’ve seen committed: give some candidates one problem and other candidates a different problem. This way a company can protect against its problems getting leaked online, right?</p><p>The issue is, as an interviewer, unless you have witnessed a dozen candidates attempt a given problem, it’s very challenging to assess their abilities. It’s easy to fall victim to the “I solved it, why can’t they” bias. You need to compare candidates to each other to assess who did a good job and who didn’t. Which brings me to my next point.</p><h2 id="Objective-Scoring"><a href="#Objective-Scoring" class="headerlink" title="Objective Scoring"></a>Objective Scoring</h2><p>You need to compare candidates objectively, scoring them on every exercise across a variety of factors. There must be well-defined criteria to differentiate scores, and scoring should be done immediately upon completion of the exercise.</p><p>Suppose our interviewer tasks the candidate to construct a simple web application. You assign a score of 1 through 4 to the following factors:</p><ol><li>Productivity</li><li>Programming Style</li><li>Language/Framework Familiarity</li><li>Debugging Ability</li></ol><p>Beforehand, you would need to write a short description of what qualifies each score for each of the above factors. Then, during the interview, you will need a rubric to help you determine what score a candidate earns for each factor. Productivity can be measured exceptionally objectively if you have the candidate follow a prescribed series of steps to solve the programming challenge. For example, at Triplebyte, we literally tell them (loosely) what functions to write and in what order.</p><p>After a week or two of interviewing a half dozen candidates, this scoring becomes critical and allows you avoid biases such as the <a href="https://en.wikipedia.org/wiki/Serial-position_effect" target="_blank" rel="noopener">Serial-position effect</a>.</p><h2 id="Culture-Fit"><a href="#Culture-Fit" class="headerlink" title="Culture Fit"></a>Culture Fit</h2><p>Surprisingly, a lot of this advice can also be applied to assessing culture fit. You can have the same person, ask the same questions, in the same order, using the same rubric to compare every answer. The risk is that you will inadvertently dehumanize the candidate, but it is possible to do this objective assessment such that the candidate is unaware of it. At the very least, you need to document your impression of the candidate immediately after the interview, lest you rely on your memory/feelings a week later when making a decision.</p><p>You could take this philosophy to the absurd, and it would certainly eliminate bias, though I personally believe there’s a point of diminishing returns, at which you degrade the experience of the interviewee. Nevertheless, some standardization across interviews is critical to avoid implicit bias and identify good candidates, and I encourage every tech company to examine their interview process to assess their level of standardization.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;I’ve conducted about 100 technical interviews over the past 6 months for a software development recruiting company called &lt;a href=&quot;https://triplebyte.com/iv/RrzqKKw/bp&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Triplebyte&lt;/a&gt;. I’ve also been doing consulting work, which has required me to take numerous technical interviews. It’s interesting contrasting the experiences to identify what works and what doesn’t.&lt;/p&gt;
&lt;p&gt;Every Triplebyte interview begins with the candidate coding a short game. We have a series of steps, and each step precisely defines simple requirements for the program to handle. I can generally tell a couple minutes into the 2 hour interview whether the candidate will be successful. There are certainly outliers (as well as mechanisms to prevent bias), but in general, I can quickly ascertain how well a candidate stacks up technically. So then, it begs the question, why is it so hard to hire engineers? The answer is, most of us are doing it wrong.&lt;/p&gt;
    
    </summary>
    
      <category term="Technical Hiring" scheme="http://nickdrane.com/categories/Technical-Hiring/"/>
    
    
  </entry>
  
  <entry>
    <title>Scraping the Web with Puppeteer: Lessons Learned</title>
    <link href="http://nickdrane.com/scraping-the-web-with-puppeteer-lessons-learned/"/>
    <id>http://nickdrane.com/scraping-the-web-with-puppeteer-lessons-learned/</id>
    <published>2017-12-09T21:35:13.000Z</published>
    <updated>2018-09-30T20:52:45.834Z</updated>
    
    <content type="html"><![CDATA[<p>I’m currently contracted to create a web service using some data from a third party Angular application. I worked off a proof of concept codebase that used Chrome’s new <a href="https://github.com/GoogleChrome/puppeteer" target="_blank" rel="noopener">Puppeteer</a> API to scrape this site. I strongly regret not starting from scratch.</p><a id="more"></a><h2 id="What-is-Puppeteer"><a href="#What-is-Puppeteer" class="headerlink" title="What is Puppeteer?"></a>What is Puppeteer?</h2><p>Puppeteer is a Node API that allows you to control Google’s headless Chrome browser. Imagine a version of your browser that can send and receive requests but has no GUI. It works in the background, performing actions as instructed by an API. This makes Puppeteer great for end to end testing a web application. You can truly simulate the user experience, typing where they type and clicking where they click. Another use case for Puppeteer is web scraping a single page web application. Let’s explore how this might work.</p><h2 id="A-Simple-Example"><a href="#A-Simple-Example" class="headerlink" title="A Simple Example"></a>A Simple Example</h2><p>For any Puppeteer project, the first task is to create an instance of the headless browser.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> browser = <span class="keyword">await</span> puppeteer.launch()</span><br><span class="line"></span><br><span class="line"><span class="comment">// We will use this page instance and it's API frequently</span></span><br><span class="line"><span class="keyword">const</span> page = <span class="keyword">await</span> <span class="keyword">this</span>.browser.newPage();</span><br></pre></td></tr></table></figure><p>After that’s done, it’s trivial to navigate to and begin interacting with a webpage. Let’s suppose I want to fill out a login form and submit an authentication request to a website. In an ideal situation, it looks like this.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Navigate to the website</span></span><br><span class="line"><span class="keyword">await</span> page.goto(<span class="string">"https://website/login"</span>);</span><br><span class="line"></span><br><span class="line"><span class="comment">// Provide the selector of an input box and the content to type</span></span><br><span class="line"><span class="keyword">await</span> page.type(<span class="string">"input#username"</span>, CREDENTIALS.username);</span><br><span class="line"><span class="keyword">await</span> page.type(<span class="string">"input#password"</span>, CREDENTIALS.password);</span><br><span class="line"><span class="keyword">await</span> page.click(<span class="string">"button#login"</span>); <span class="comment">// Click the login button</span></span><br><span class="line"></span><br><span class="line"><span class="comment">// Wait until the screen changes and a node matching</span></span><br><span class="line"><span class="comment">// the selector #logged-in-successfully appears,</span></span><br><span class="line"><span class="comment">// at which point we know the login was successful</span></span><br><span class="line"><span class="keyword">await</span> page.waitForSelector(<span class="string">"#logged-in-successfully"</span>);</span><br></pre></td></tr></table></figure><p>We just successfully filled out a form, submitted an HTTP request containing our form data, and waited for the page to change upon successful login. This is where Puppeteer shines. Let’s look at a more complicated example.</p><h2 id="A-More-Complicated-Example"><a href="#A-More-Complicated-Example" class="headerlink" title="A More Complicated Example"></a>A More Complicated Example</h2><p>Today’s web uses a mix of simple html driven forms as well as more complicated, javascript-driven forms, rich with functionality like autocomplete and dynamic dropdown menus. I’m sure you’ve used a calendar date picker. These components each need to be treated differently.</p><p>Here is a modified version of some code I wrote for my client:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br><span class="line">46</span><br><span class="line">47</span><br><span class="line">48</span><br><span class="line">49</span><br><span class="line">50</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// The form inputs I want to fill out</span></span><br><span class="line"><span class="comment">// and their corresponding selectors</span></span><br><span class="line"><span class="keyword">const</span> fields = &#123;</span><br><span class="line">  type: <span class="string">'input[ng-model="type"]'</span>,</span><br><span class="line">  origin: <span class="string">'input[ng-model="origin"]'</span>,</span><br><span class="line">  destination: <span class="string">'input[ng-model="destination"]'</span>,</span><br><span class="line">  date: <span class="string">'input[ng-model="date"]'</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">search</span>(<span class="params">searchParams</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> page = <span class="keyword">await</span> <span class="keyword">this</span>.browser.newPage();</span><br><span class="line">  <span class="keyword">await</span> page.goto(<span class="string">"https://website/search"</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// We need to click a button to make the search form</span></span><br><span class="line">  <span class="comment">// appear, but first make sure that button has rendered</span></span><br><span class="line">  <span class="keyword">await</span> page.waitForSelector(<span class="string">".new-search"</span>);</span><br><span class="line">  <span class="keyword">await</span> page.click(<span class="string">".new-search"</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// Fill out the form</span></span><br><span class="line">  <span class="keyword">for</span> (<span class="keyword">const</span> field <span class="keyword">of</span> <span class="built_in">Object</span>.keys(fields)) &#123;</span><br><span class="line">    <span class="keyword">if</span> (searchParams[field]) &#123;</span><br><span class="line">      <span class="keyword">const</span> selector = fields[field];</span><br><span class="line"></span><br><span class="line">      <span class="comment">// We want to make sure each DOM node is rendered</span></span><br><span class="line">      <span class="comment">// before we try to do anything to it.</span></span><br><span class="line">      <span class="keyword">await</span> page.waitForSelector(selector);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Some inputs need to be focused first for page.type to work</span></span><br><span class="line">      <span class="comment">// Might as well focus on all of them</span></span><br><span class="line">      <span class="keyword">await</span> page.focus(selector);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// Some inputs have defaults that need to be</span></span><br><span class="line">      <span class="comment">// erased before typing your own input</span></span><br><span class="line">      <span class="keyword">if</span> (field === <span class="string">"date"</span> || field === <span class="string">"type"</span>) &#123;</span><br><span class="line">        <span class="comment">// This is a helper function I wrote</span></span><br><span class="line">        <span class="keyword">await</span> deleteInput(page, selector);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      <span class="keyword">await</span> page.type(selector, searchParams[field]);</span><br><span class="line"></span><br><span class="line">      <span class="comment">// This field won't register the typed data</span></span><br><span class="line">      <span class="comment">// until Enter is pressed.</span></span><br><span class="line">      <span class="comment">// This is because the 'type' field is a dropdown</span></span><br><span class="line">      <span class="comment">// where one of a specific set of inputs must be clicked.</span></span><br><span class="line">      <span class="keyword">if</span> (field === <span class="string">"type"</span>) &#123;</span><br><span class="line">        <span class="keyword">await</span> page.keyboard.press(<span class="string">"Enter"</span>);</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The first thing to notice is that the code is messy and full of weird exceptions. To make matters worse, it doesn’t always work.</p><h2 id="Brace-Yourself-for-Flaky-Behavior"><a href="#Brace-Yourself-for-Flaky-Behavior" class="headerlink" title="Brace Yourself for Flaky Behavior"></a>Brace Yourself for Flaky Behavior</h2><p>Working with Puppeteer was an exercise in guesswork. Given the same inputs, Puppeteer did not always produce the same outputs<sup><a href="#footnote1">1</a></sup>. This flaky behavior made the project unnecessarily challenging and required me to do additional engineering to increase reliability, which was frustrating considering the alternative.</p><h2 id="Puppeteer-Was-Completely-Unnecessary"><a href="#Puppeteer-Was-Completely-Unnecessary" class="headerlink" title="Puppeteer Was Completely Unnecessary"></a>Puppeteer Was Completely Unnecessary</h2><p>Puppeteer has a API that allows you to execute arbitrary code against the DOM. After scraping form results with this API and getting the same flaky behavior described above, I ditched the approach and started grabbing data from the HTTP response objects themselves. Below is a primitive version of some code I wrote to do this.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br></pre></td><td class="code"><pre><span class="line">waitForUrl(page, urlPrefix) &#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="keyword">new</span> <span class="built_in">Promise</span>(<span class="function"><span class="params">resolve</span> =&gt;</span> &#123;</span><br><span class="line">    page.on(<span class="string">"response"</span>, res =&gt; &#123;</span><br><span class="line">      <span class="keyword">if</span> (!res.url.startsWith(urlPrefix)) &#123;</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">      &#125;</span><br><span class="line">      resolve(res.json());</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The page passed into this function is the same page object created above by the Puppeteer API. Among other things, it is an event emitter that allows me to listen for any HTTP responses. I’ve essentially created a promise that resolves to the response body of a particular ajax request. This allowed me interact with the server API directly and removed the DOM from the data retrieval process, greatly reducing the chance for flaky behavior. But that begs the question, why use Puppeteer at all? Why not simply send http requests to the server API manually and ditch the complicated form submission code above That’s how I should have started all along.</p><h2 id="When-is-Puppeteer-the-Right-Solution"><a href="#When-is-Puppeteer-the-Right-Solution" class="headerlink" title="When is Puppeteer the Right Solution"></a>When is Puppeteer the Right Solution</h2><p>I can only think of a single scenario where using Puppeteer for scraping is superior to the alternative: if the information you want is generated using a combination of API data and javascript code. After all, you would have no other way to simulate the javascript code without rewriting it.</p><p>If, however, all you need is data from the server, go the simple route and hit the API with an HTTP library like <a href="https://github.com/axios/axios" target="_blank" rel="noopener">axios</a> or <a href="https://github.com/request/request" target="_blank" rel="noopener">request</a>. If you are scraping a server side rendered application, you can combine one of the aforementioned tools with <a href="https://github.com/cheeriojs/cheerio" target="_blank" rel="noopener">Cheerio</a>, giving you a far more user friendly DOM manipulation API than that offered my Puppeteer.</p><h4 id="Footnotes"><a href="#Footnotes" class="headerlink" title="Footnotes"></a>Footnotes</h4><p><a name="footnote1">1</a>: You might be curious why Puppeteer exhibited flaky behavior. One issue that I ran into was animations. I might attempt to click a DOM node, but if the animation had not finished, the click would not register. In essence, it would appear as if the click had worked, but once the animation finished, the DOM would reset itself, undoing my click. I think this is simply how Angular’s digest loop reacted to a click at an unexpected time. Unfortunately, Puppeteer provides no functionality to determine when an animation has finished (after all, how would it!). I tried a couple solutions. One entailed a sleep function to wait a couple hundred milliseconds for the animation to finish, but it simply exacerbated the flaky behavior. A second involved executing the click only once the DOM node had a particular class that indicated the animation had finished. At one point, I even tried to disable all animations across the website. All these solutions were half-baked.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;I’m currently contracted to create a web service using some data from a third party Angular application. I worked off a proof of concept codebase that used Chrome’s new &lt;a href=&quot;https://github.com/GoogleChrome/puppeteer&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;Puppeteer&lt;/a&gt; API to scrape this site. I strongly regret not starting from scratch.&lt;/p&gt;
    
    </summary>
    
      <category term="Javascript" scheme="http://nickdrane.com/categories/Javascript/"/>
    
      <category term="Failures" scheme="http://nickdrane.com/categories/Failures/"/>
    
      <category term="Web Scraping" scheme="http://nickdrane.com/categories/Web-Scraping/"/>
    
    
  </entry>
  
  <entry>
    <title>Regex And Automated Test Fuzzing</title>
    <link href="http://nickdrane.com/regex-and-automated-test-fuzzing/"/>
    <id>http://nickdrane.com/regex-and-automated-test-fuzzing/</id>
    <published>2017-12-06T06:00:00.000Z</published>
    <updated>2018-10-18T03:20:23.622Z</updated>
    
    <content type="html"><![CDATA[<p>I posted my article <a href="https://nickdrane.com/build-your-own-regex/">Build Your Own Regex Engine</a> on Reddit the other day, and one of the commenters claimed that the implementation should be trivial to break. Since I had already tested my program against a customized suite of tests, the remark got me thinking about how I could further increase my confidence in the correctness of my program. One extremely low cost however effective strategy for identifying faults in software is known as fuzzing.</p><a id="more"></a><h2 id="What-is-Fuzzing"><a href="#What-is-Fuzzing" class="headerlink" title="What is Fuzzing?"></a>What is Fuzzing?</h2><p>Fuzzing is a automated testing technique where a program is provided a series of invalid or randomly generated inputs. If we were testing an HTTP API, we might send randomized combinations of query parameters and ensure that our server always returns a 2xx status code. Since Javascript comes with a regular expression engine, my fuzzer asserts that given the same random input, both engine’s return the same output.</p><h2 id="Specifying-the-Grammar"><a href="#Specifying-the-Grammar" class="headerlink" title="Specifying the Grammar"></a>Specifying the Grammar</h2><p>The first step is to specify the grammar that our regex engine supports.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> lowercase = <span class="string">"abcdefghijklmnopqrstuvwxyz"</span>.split(<span class="string">""</span>);</span><br><span class="line"><span class="keyword">const</span> uppercase = lowercase.map(<span class="function"><span class="params">letter</span> =&gt;</span> letter.toUpperCase());</span><br><span class="line"><span class="keyword">const</span> special = [<span class="string">"?"</span>, <span class="string">"*"</span>, <span class="string">"."</span>];</span><br><span class="line"><span class="keyword">const</span> regexGrammar = special.concat(lowercase, uppercase);</span><br></pre></td></tr></table></figure><p>You might notice we skipped the <code>^</code> and <code>$</code> characters. More on these in a little bit.</p><h2 id="Generated-Valid-Regular-Expressions"><a href="#Generated-Valid-Regular-Expressions" class="headerlink" title="Generated Valid Regular Expressions"></a>Generated Valid Regular Expressions</h2><p>We want to write a function <code>generateRegex</code> that will select <code>n</code> random characters from the <code>regexGrammar</code> and concatenate them together into a string. This string will be used to create a test regex.</p><p>Here are three possible returns values of <code>generateRegex</code>:</p><ol><li><code>.AnrQ?QNLQX.syBsOcJlbJZd</code></li><li><code>.LkuZ?Ynj</code></li><li><code>.UN?eiyddhXvyNj</code></li></ol><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">generateRegex</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> regexString = <span class="keyword">new</span> <span class="built_in">Array</span>(n)</span><br><span class="line">    .fill(<span class="number">0</span>)</span><br><span class="line">    .map(chooseOne)</span><br><span class="line">    .join(<span class="string">""</span>);</span><br><span class="line"></span><br><span class="line">  <span class="keyword">return</span> regexString;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Pick one element randomly from the grammar and return it</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">chooseOne</span>(<span class="params"></span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> regexGrammar[<span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * regexGrammar.length)];</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Removing-Invalid-Regex-Strings"><a href="#Removing-Invalid-Regex-Strings" class="headerlink" title="Removing Invalid Regex Strings"></a>Removing Invalid Regex Strings</h2><p>My regex engine only deals with a very small subset of available regex syntax, and furthermore, it does not contain any error handling. What happens if <code>generateRegex</code> returns the pattern <code>**</code> or <code>^*</code>? My regex engine was never designed to handle these inputs, though they are possible outputs of <code>generateRegex</code>. We need to make a choice about how to handle these expressions. Since the primary goal of my regex engine is accessibility and simplicity of implementation, I’m not about to begin supporting these edge cases. That means my fuzzer should not generate them either.</p><p>One solution to determine if a given regex string is valid is to specify my regex engine’s allowable grammar in <a href="https://en.wikipedia.org/wiki/Backus%E2%80%93Naur_form" target="_blank" rel="noopener">BNF</a>. BNF is a formal notation for specifying the syntax of a language. Given this BNF notation, I could ask another program if the randomly generated regex string can be created using my BNF specification. This sounds like a little more work than I want, however, since the invalid cases can simply be manually enumerated and filtered.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">validRegex</span>(<span class="params">regexString</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (</span><br><span class="line">    <span class="comment">// None of the following sequences are properly</span></span><br><span class="line">    <span class="comment">// defined by my regex engine</span></span><br><span class="line">    regexString.indexOf(<span class="string">"**"</span>) === <span class="number">-1</span> &amp;&amp;</span><br><span class="line">    regexString.indexOf(<span class="string">"??"</span>) === <span class="number">-1</span> &amp;&amp;</span><br><span class="line">    regexString.indexOf(<span class="string">"*?"</span>) === <span class="number">-1</span> &amp;&amp;</span><br><span class="line">    regexString.indexOf(<span class="string">"?*"</span>) === <span class="number">-1</span> &amp;&amp;</span><br><span class="line">    regexString.indexOf(<span class="string">"^?"</span>) === <span class="number">-1</span> &amp;&amp;</span><br><span class="line">    regexString.indexOf(<span class="string">"^*"</span>) === <span class="number">-1</span> &amp;&amp;</span><br><span class="line">    !regexString.startsWith(<span class="string">"*"</span>) &amp;&amp;</span><br><span class="line">    !regexString.startsWith(<span class="string">"?"</span>)</span><br><span class="line">  );</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">generateRegex</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">let</span> regexString = <span class="keyword">new</span> <span class="built_in">Array</span>(n)</span><br><span class="line">    .fill(<span class="number">0</span>)</span><br><span class="line">    .map(chooseOne)</span><br><span class="line">    .join(<span class="string">""</span>);</span><br><span class="line"></span><br><span class="line">  <span class="comment">// If the generated string is valid, return it</span></span><br><span class="line">  <span class="keyword">if</span> (validRegex(regexString)) &#123;</span><br><span class="line">    <span class="keyword">return</span> regexString;</span><br><span class="line">  <span class="comment">// Otherwise generate a new string and return that</span></span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> generateRegexString(n);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>One more modification to <code>generateRegex</code> is necessary to support <code>^</code> and <code>$</code>, and then we are basically done.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">generateRegex</span>(<span class="params">n</span>) </span>&#123;</span><br><span class="line">  ...</span><br><span class="line">  <span class="comment">// We need to ensure that '^' and '$' only go at the beginning</span></span><br><span class="line">  <span class="comment">// and the end of the string, respectively.</span></span><br><span class="line">  <span class="comment">// Give each a 10% probability of appearing in a string</span></span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">Math</span>.random() &lt; <span class="number">0.1</span>) regexString = <span class="string">"^"</span> + regexString;</span><br><span class="line">  <span class="keyword">if</span> (<span class="built_in">Math</span>.random() &lt; <span class="number">0.1</span>) regexString = regexString + <span class="string">"$"</span>;</span><br><span class="line">  ...</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Comparing-Regex-Implementations"><a href="#Comparing-Regex-Implementations" class="headerlink" title="Comparing Regex Implementations"></a>Comparing Regex Implementations</h2><p>All that is required now is to repeatedly invoke <code>generateRegex</code> a fixed number of times and then compare the output of the native JS implementation with the output of my implementation.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// The corpus is the string of text we are matching the pattern against.</span></span><br><span class="line"><span class="comment">// I used a segment of Gulliver's Travels from Project Gutenberg.</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">fuzzer</span>(<span class="params">totalTests, corpus</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">const</span> maxRegexLength = <span class="number">50</span>; <span class="comment">// max will actually be 50 - 1</span></span><br><span class="line">  <span class="keyword">let</span> testsRun = <span class="number">0</span>;</span><br><span class="line">  <span class="keyword">while</span> (testsRun &lt; totalTests) &#123;</span><br><span class="line">    <span class="keyword">const</span> regexLength = getRandomInt(<span class="number">1</span>, maxRegexLength);</span><br><span class="line">    <span class="keyword">const</span> regexString = generateRegexString(regexLength);</span><br><span class="line">    <span class="keyword">const</span> testRegex = <span class="keyword">new</span> <span class="built_in">RegExp</span>(regexString);</span><br><span class="line">    <span class="keyword">try</span> &#123;</span><br><span class="line">      assert.equal(testRegex.test(corpus), myRegexEngine(regexString, corpus));</span><br><span class="line">    &#125; <span class="keyword">catch</span> (err) &#123;</span><br><span class="line">      <span class="built_in">console</span>.log(testRegex);</span><br><span class="line">    &#125;</span><br><span class="line">    testsRun++;</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// Thank you Mozzila :)</span></span><br><span class="line"><span class="comment">// https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Math/random</span></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">getRandomInt</span>(<span class="params">min, max</span>) </span>&#123;</span><br><span class="line">  min = <span class="built_in">Math</span>.ceil(min);</span><br><span class="line">  max = <span class="built_in">Math</span>.floor(max);</span><br><span class="line">  <span class="keyword">return</span> <span class="built_in">Math</span>.floor(<span class="built_in">Math</span>.random() * (max - min)) + min;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Results"><a href="#Results" class="headerlink" title="Results"></a>Results</h2><p>I ran my fuzzer for a couple million randomly generated cases and ended up learning two things about my regex engine.</p><ol><li><p>My implementation fails extraordinarily with longer texts. I knew recursion would be a problem for any practical regex implementation (at least without <a href="https://en.wikipedia.org/wiki/Tail_call" target="_blank" rel="noopener">tail calls</a>) and would cause stack overflows, but I didn’t expect it to fail with texts that were only a couple thousand words. I think this is because I make liberal use of backtracking algorithms in <code>matchQuestion</code> and <code>matchStar</code>. Since I was forced to test with a relatively short input text, it makes sense to use multiple text inputs to increase the probability of discovering an error.</p></li><li><p>My implementation treats the <code>.</code> character differently than the native implementation. In the RegExp implementation, <code>.</code> will not match various line terminators (<code>\n</code>, <code>\r</code>, <code>\u2028</code> or <code>\u2029</code>). My implementation does.</p></li></ol><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>The biggest takeaway is that fuzzing is an simple and inexpensive way to enumerate enormous sets of inputs and identify bugs in your software. This fuzzer took less than an hour to write.</p><p>But remember, this fuzzer’s blessing of a couple million input combinations <strong>does not</strong> verify the correctness of my program. Not even close. A fuzzer is a tool to identify potential errors. Unless you enumerate all possible inputs (completely impossible in this case where they are infinite), you are not guaranteed your program is error free.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;I posted my article &lt;a href=&quot;https://nickdrane.com/build-your-own-regex/&quot;&gt;Build Your Own Regex Engine&lt;/a&gt; on Reddit the other day, and one of the commenters claimed that the implementation should be trivial to break. Since I had already tested my program against a customized suite of tests, the remark got me thinking about how I could further increase my confidence in the correctness of my program. One extremely low cost however effective strategy for identifying faults in software is known as fuzzing.&lt;/p&gt;
    
    </summary>
    
      <category term="Javascript" scheme="http://nickdrane.com/categories/Javascript/"/>
    
      <category term="Regular Expressions" scheme="http://nickdrane.com/categories/Regular-Expressions/"/>
    
      <category term="Testing" scheme="http://nickdrane.com/categories/Testing/"/>
    
    
  </entry>
  
  <entry>
    <title>Build a Regex Engine in Less than 40 Lines of Code</title>
    <link href="http://nickdrane.com/build-your-own-regex/"/>
    <id>http://nickdrane.com/build-your-own-regex/</id>
    <published>2017-11-28T17:36:04.000Z</published>
    <updated>2018-10-18T03:20:23.620Z</updated>
    
    <content type="html"><![CDATA[<p>I stumbled upon an <a href="https://www.cs.princeton.edu/courses/archive/spr09/cos333/beautiful.html" target="_blank" rel="noopener">article</a> the other day where Rob Pike implements a rudimentary regular expression engine in c. I converted his code to Javascript and added test specs so that someone can self-guide themselves through the creation of the regex engine. The specs and solution can be found in this <a href="https://github.com/nadrane/build-your-own-regex" target="_blank" rel="noopener">GitHub repository</a>. This blog post walks through my solution.</p><a id="more"></a><h2 id="The-Problem"><a href="#The-Problem" class="headerlink" title="The Problem"></a>The Problem</h2><p>Our regex engine will support the following syntax:</p><table><thead><tr><th>Syntax</th><th>Meaning</th><th>Example</th><th>matches</th></tr></thead><tbody><tr><td>a</td><td>Matches the specified character literal</td><td>q</td><td>q</td></tr><tr><td>*</td><td>Matches 0 or more of the previous character</td><td>a*</td><td>“”, a, aa, aaa</td></tr><tr><td>?</td><td>Matches 0 or 1 of the previous character</td><td>a?</td><td>“”, a</td></tr><tr><td>.</td><td>Matches any character literal</td><td>.</td><td>a, b, c, d, e …</td></tr><tr><td>^</td><td>Matches the start of a string</td><td>^c</td><td>c, ca, caa, cbb …</td></tr><tr><td>$</td><td>Matches the end of a string</td><td>a$</td><td>ba, baaa, qwerta …</td></tr></tbody></table><p>The goal is to provide a syntax robust enough to match a large portion of regex use cases with minimal code.</p><h2 id="Matching-One-Character"><a href="#Matching-One-Character" class="headerlink" title="Matching One Character"></a>Matching One Character</h2><p>The first step is to write a function that takes in a one character pattern and a one character text string and returns a boolean indicating if they match. A pattern of <code>.</code> is considered a wildcard and matches against any character literal.</p><p>Here are some examples</p><p><code>matchOne(&#39;a&#39;, &#39;a&#39;)</code> -&gt; <code>true</code><br><code>matchOne(&#39;.&#39;, &#39;z&#39;)</code> -&gt; <code>true</code><br><code>matchOne(&#39;&#39;, &#39;h&#39;)</code>  -&gt; <code>true</code><br><code>matchOne(&#39;a&#39;, &#39;b&#39;)</code> -&gt; <code>false</code><br><code>matchOne(&#39;p&#39;, &#39;&#39;)</code>  -&gt; <code>false</code></p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">matchOne</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (!pattern) <span class="keyword">return</span> <span class="literal">true</span> <span class="comment">// Any text matches an empty pattern</span></span><br><span class="line">  <span class="keyword">if</span> (!text) <span class="keyword">return</span> <span class="literal">false</span>   <span class="comment">// If the pattern is defined but the text is empty, there cannot be a match</span></span><br><span class="line">  <span class="keyword">if</span> (pattern === <span class="string">"."</span>) <span class="keyword">return</span> <span class="literal">true</span> <span class="comment">// Any inputted text matches the wildcard</span></span><br><span class="line">  <span class="keyword">return</span> pattern === text</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Matching-Same-Length-Strings"><a href="#Matching-Same-Length-Strings" class="headerlink" title="Matching Same Length Strings"></a>Matching Same Length Strings</h2><p>Now we want to add support for patterns and text strings of greater length. For now, let’s only consider a pattern/text pair of the same length. I happen to know that the solution lends itself very naturally to recursion, so we will use it here. We are going to want to repeatedly invoke <code>matchOne</code> on successive pairs of characters from the pattern/text combination.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">match</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pattern === <span class="string">""</span>) <span class="keyword">return</span> <span class="literal">true</span>  <span class="comment">// Our base case - if the pattern is empty, any inputted text is a match</span></span><br><span class="line">  <span class="keyword">else</span> <span class="keyword">return</span> matchOne(pattern[<span class="number">0</span>], text[<span class="number">0</span>]) &amp;&amp; match(pattern.slice(<span class="number">1</span>), text.slice(<span class="number">1</span>))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The above code advances character by character across the the pattern/text pair. It first compares <code>pattern[0]</code> to <code>text[0]</code> and then <code>pattern[1]</code> to <code>text[1]</code> and continues comparing <code>pattern[i]</code> to <code>text[i]</code> until <code>i === pattern.length - 1</code>. If they ever don’t match, then we know that the pattern cannot match the text.</p><p>Let’s take an example. Suppose we invoke <code>match(&#39;a.c&#39;, &#39;abc&#39;)</code>, which returns <code>matchOne(&#39;a&#39;, &#39;a&#39;) &amp;&amp; match(&#39;.c&#39;, &#39;bc&#39;)</code>.</p><p>If we continue evaluating these functions, we get <code>matchOne(&#39;a&#39;, &#39;a&#39;) &amp;&amp; matchOne(&#39;.&#39;, &#39;b&#39;) &amp;&amp; matchOne(&#39;c&#39;, &#39;c&#39;) &amp;&amp; match(&quot;&quot;, &quot;&quot;)</code>, which is just equal to <code>true &amp;&amp; true &amp;&amp; true &amp;&amp; true</code>, So we have a match!</p><h2 id="The-Character"><a href="#The-Character" class="headerlink" title="The $ Character"></a>The $ Character</h2><p>Let’s add support for the special pattern character <code>$</code> that allows us to match the end of a string. The solution simply requires adding an additional base case to the match function.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">match</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pattern === <span class="string">""</span>) <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  <span class="keyword">if</span> (pattern === <span class="string">"$"</span> &amp;&amp; text === <span class="string">""</span>) <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  <span class="keyword">else</span> <span class="keyword">return</span> matchOne(pattern[<span class="number">0</span>], text[<span class="number">0</span>]) &amp;&amp; match(pattern.slice(<span class="number">1</span>), text.slice(<span class="number">1</span>))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="The-Character-1"><a href="#The-Character-1" class="headerlink" title="The ^ Character"></a>The ^ Character</h2><p>Let’s add support for the special pattern character <code>^</code> that allows us to match the beginning of a string. I’m going to introduce a new function called <code>search</code>.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">search</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pattern[<span class="number">0</span>] === <span class="string">"^"</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> match(pattern.slice(<span class="number">1</span>), text)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>This function will be the new entry point to our code. Up till this point, we were only matching patterns that began at the beginning of the text. We are simply making that more clear now by forcing the user to preface the pattern with a <code>^</code>. But how do we support patterns that appear anywhere within the text?</p><h2 id="Matches-Starting-Anywhere"><a href="#Matches-Starting-Anywhere" class="headerlink" title="Matches Starting Anywhere"></a>Matches Starting Anywhere</h2><p>Currently, the following return <code>true</code></p><p><code>search(&quot;^abc&quot;, &quot;abc&quot;)</code><br><code>search(&quot;^abcd&quot;, &quot;abcd&quot;)</code></p><p>But <code>search(&quot;bc&quot;, &quot;abcd&quot;)</code> will just return <code>undefined</code>. We want it to return <code>true</code></p><p>If the user does not specify that the pattern matches the beginning of the text, then we want to search for that pattern at every possible starting point within the text. We will default to this behavior if the pattern does not begin with <code>^</code><sup><a href="#footnote1">1</a></sup>.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">search</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pattern[<span class="number">0</span>] === <span class="string">"^"</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> match(pattern.slice(<span class="number">1</span>), text)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="comment">// This code will run match(pattern, text.slice(index)) on every index of the text.</span></span><br><span class="line">    <span class="comment">// This means that we test the pattern against every starting point of the text.</span></span><br><span class="line">    <span class="keyword">return</span> text.split(<span class="string">""</span>).some(<span class="function">(<span class="params">_, index</span>) =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">return</span> match(pattern, text.slice(index))</span><br><span class="line">    &#125;)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="The-Character-2"><a href="#The-Character-2" class="headerlink" title="The ? Character"></a>The ? Character</h2><p>We want to be able to match 0 to 1 of the character before <code>?</code>.</p><p>Here are some examples</p><p><code>search(&quot;ab?c&quot;, &quot;ac&quot;)</code>    -&gt; <code>true</code><br><code>search(&quot;ab?c&quot;, &quot;abc&quot;)</code>   -&gt; <code>true</code><br><code>search(&quot;a?b?c?&quot;, &quot;abc&quot;)</code> -&gt; <code>true</code><br><code>search(&quot;a?b?c?&quot;, &quot;&quot;)</code>    -&gt; <code>true</code></p><p>The first step is to modify <code>match</code> to detect when a <code>?</code> character is present and then delegate to the <code>matchQuestion</code> function, which we will define shortly.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">match</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pattern === <span class="string">""</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pattern === <span class="string">"$"</span> &amp;&amp; text === <span class="string">""</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  <span class="comment">// Notice that we are looking at pattern[1] instead of pattern[0].</span></span><br><span class="line">  <span class="comment">// pattern[0] is the character to match 0 or 1 of.</span></span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pattern[<span class="number">1</span>] === <span class="string">"?"</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> matchQuestion(pattern, text)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> matchOne(pattern[<span class="number">0</span>], text[<span class="number">0</span>]) &amp;&amp; match(pattern.slice(<span class="number">1</span>), text.slice(<span class="number">1</span>))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>matchQuestion</code> needs to handle two cases:</p><ol><li>Where the character before the <code>?</code> is not matched but the text matches the remainder of the pattern (everything after the <code>?</code>).</li><li>Where the character before the <code>?</code> is matched and the rest of the text (minus the 1 matched character) matches the remainder of the pattern.</li></ol><p>If either of these cases is truthy, then <code>matchQuestion</code> can return <code>true</code>.</p><p>Let’s consider the first case. How do we check if the text matches everything in the pattern except the <code>_?</code> syntax? In order words, how do we check if the character before the <code>?</code> appears 0 times? We strip 2 characters off the pattern (the first character is the one before the <code>?</code> and the second is the <code>?</code> itself) and invoke the match function.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">matchQuestion</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> match(pattern.slice(<span class="number">2</span>), text);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The second case is a little more challenging, but just like before, it reuses functions we’ve already written</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">matchQuestion</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (matchOne(pattern[<span class="number">0</span>], text[<span class="number">0</span>]) &amp;&amp; match(pattern.slice(<span class="number">2</span>), text.slice(<span class="number">1</span>))) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> match(pattern.slice(<span class="number">2</span>), text);</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>If the <code>text[0]</code> matches <code>pattern[0]</code>, and the rest of the text (minus the part that is matched by <code>matchOne</code>) matches the remainder of the pattern, then we are golden. Note that we could rewrite the code like this:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">matchQuestion</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (matchOne(pattern[<span class="number">0</span>], text[<span class="number">0</span>]) &amp;&amp; match(pattern.slice(<span class="number">2</span>), text.slice(<span class="number">1</span>))) || match(pattern.slice(<span class="number">2</span>), text);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The one thing I like about this latter approach is that the boolean OR makes it explicitly clear that there are two cases, either of which may be true.</p><h2 id="The-Character-3"><a href="#The-Character-3" class="headerlink" title="The * Character"></a>The * Character</h2><p>We want to be able to match the character before the <code>*</code> 0 or more times.</p><p>All of these should return <code>true</code>.</p><p><code>search(&quot;a*&quot;, &quot;&quot;)</code><br><code>search(&quot;a*&quot;, &quot;aaaaaaa&quot;)</code><br><code>search(&quot;a*b&quot;, &quot;aaaaaaab&quot;)</code></p><p>Similar to what we did when supporting <code>?</code>, we wan to delegate to a <code>matchStar</code> function within our <code>match</code> function</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">match</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pattern === <span class="string">""</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pattern === <span class="string">"$"</span> &amp;&amp; text === <span class="string">""</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span></span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pattern[<span class="number">1</span>] === <span class="string">"?"</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> matchQuestion(pattern, text)</span><br><span class="line">  &#125; <span class="keyword">else</span> <span class="keyword">if</span> (pattern[<span class="number">1</span>] === <span class="string">"*"</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> matchStar(pattern, text)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> matchOne(pattern[<span class="number">0</span>], text[<span class="number">0</span>]) &amp;&amp; match(pattern.slice(<span class="number">1</span>), text.slice(<span class="number">1</span>))</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>matchStar</code>, like <code>matchQuestion</code>, also needs to handle two cases:</p><ol><li>Where the character before the <code>*</code> is not matched but the text matches the remainder of the pattern (everything after the <code>*</code>).</li><li>Where the character before the <code>*</code> is matched one or more times and the rest of the text matches the remainder of the pattern.</li></ol><p>Since there are two cases that both result in a match (0 matches OR more matches), we know that <code>matchStar</code> can be implemented with a boolean OR. Furthermore, case 1 for <code>matchStar</code> is exactly the same as it was for <code>matchQuestion</code> and can be implemented identically using <code>match(pattern.slice(2), text)</code>. That means we only need to formulate an expression that satisfies case 2.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">matchStar</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> (matchOne(pattern[<span class="number">0</span>], text[<span class="number">0</span>]) &amp;&amp; match(pattern, text.slice(<span class="number">1</span>))) || match(pattern.slice(<span class="number">2</span>), text);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="Refactoring"><a href="#Refactoring" class="headerlink" title="Refactoring"></a>Refactoring</h2><p>We can now go back and cleverly simplify <code>search</code> using a trick I learned in Peter Norvig’s <a href="https://www.udacity.com/course/design-of-computer-programs--cs212" target="_blank" rel="noopener">class</a>.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">search</span>(<span class="params">pattern, text</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">if</span> (pattern[<span class="number">0</span>] === <span class="string">"^"</span>) &#123;</span><br><span class="line">    <span class="keyword">return</span> match(pattern.slice(<span class="number">1</span>), text)</span><br><span class="line">  &#125; <span class="keyword">else</span> &#123;</span><br><span class="line">    <span class="keyword">return</span> match(<span class="string">".*"</span> + pattern, text)</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>We use the <code>*</code> character itself to allow for the pattern to appear anywhere in the string. The prepended <code>.*</code> says that any number of any character can appear before the pattern we wish to match.</p><h2 id="Conclusion"><a href="#Conclusion" class="headerlink" title="Conclusion"></a>Conclusion</h2><p>It’s remarkable how simple and elegant the code for such a sophisticated and generalized program can be. The full source is available in this <a href="https://github.com/nadrane/build-your-own-regex" target="_blank" rel="noopener">GitHub repository</a></p><p>Here is a <a href="https://nickdrane.com/regex-and-automated-test-fuzzing/">follow up article</a> where I fuzz test the regex engine.</p><h4 id="Footnotes"><a href="#Footnotes" class="headerlink" title="Footnotes"></a>Footnotes</h4><p><a name="footnote1">1</a>: There is a small bug in this code that I’m choosing to ignore. We don’t account for the case that text is an empty string. Currently when <code>text === &#39;&#39;</code>, <code>text.split(&quot;&quot;)</code> will return <code>[]</code> and will not appropriately call <code>match</code>.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;I stumbled upon an &lt;a href=&quot;https://www.cs.princeton.edu/courses/archive/spr09/cos333/beautiful.html&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;article&lt;/a&gt; the other day where Rob Pike implements a rudimentary regular expression engine in c. I converted his code to Javascript and added test specs so that someone can self-guide themselves through the creation of the regex engine. The specs and solution can be found in this &lt;a href=&quot;https://github.com/nadrane/build-your-own-regex&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;GitHub repository&lt;/a&gt;. This blog post walks through my solution.&lt;/p&gt;
    
    </summary>
    
      <category term="Javascript" scheme="http://nickdrane.com/categories/Javascript/"/>
    
      <category term="Regular Expressions" scheme="http://nickdrane.com/categories/Regular-Expressions/"/>
    
      <category term="Build Your Own" scheme="http://nickdrane.com/categories/Build-Your-Own/"/>
    
      <category term="Recursion" scheme="http://nickdrane.com/categories/Recursion/"/>
    
    
  </entry>
  
  <entry>
    <title>Optimizing Elasticsearch Score: How to Rank and Differentiate Similar Records</title>
    <link href="http://nickdrane.com/optimizing-elasticsearch-score/"/>
    <id>http://nickdrane.com/optimizing-elasticsearch-score/</id>
    <published>2017-10-11T05:00:00.000Z</published>
    <updated>2018-10-12T03:21:08.438Z</updated>
    
    <content type="html"><![CDATA[<p>A client approached me with a puzzling problem:</p><p>At Fraight, we have an omnisearch interface backed by an Elasticsearch datastore. The interface allows users yo type a freetext query and get a list of database records sorted by relevancy. At it’s core, this is a simple problem: if the user types in <code>Joe</code>, return all people whose name contains the word <code>Joe</code>. And indeed, returning all the <code>Joe&#39;s</code> in the system is trivial; the problem is that we worked hundreds, possibly even thousands of <code>Joes</code>. How do we identify the particular <code>Joe</code> that we care about?</p><a id="more"></a><h2 id="A-Poor-Solution"><a href="#A-Poor-Solution" class="headerlink" title="A Poor Solution"></a>A Poor Solution</h2><p>When I worked at <a href="https://www.epic.com/" target="_blank" rel="noopener">Epic</a>, we had a similar problem. We had a search interface that allowed us to look up patients. Unfortunately, our full names are not as unique as we like to believe, and a simple query for <code>Luke Smith</code> would surely bring the system to a halt. Epic solved this problem by providing additional fields. That’s why (along with HIPPA reasons), when you call the doctor, they might ask for your birthdate or your address; this additional identifying information is used to pare down the search results. This solution is slow, cumbersome and was deemed wholly inadequate for us.</p><h2 id="Fraight’s-Solution"><a href="#Fraight’s-Solution" class="headerlink" title="Fraight’s Solution"></a>Fraight’s Solution</h2><p>We settled on two attributes that should influence the score of a particular record:</p><ol><li>How often we interact with an entity</li><li>How recently we’ve interacted with an entity</li></ol><p>It’s important to know that most of the trucking organizations in our system have been worked with minimally. We needed a remove this noise from the search results. Phrased differently, if we regularly work with a particular <code>Great America Truckers</code> more than the other 1000 <code>Great America Truckers</code>, we want our partner to appear higher in the search results.</p><p>Similarly, if we have recently interacted with an organization, there’s a good chance we will need to interact with them in the future. For example, if we’ve just initiated a conversation with a new business partner, there’s a good chance we will continue interacting with them regularly in the near-term. It’s important, however, to consider the time since our last interaction. If we interacted with someone yesterday or the day before, it’s very important for them to be ranked higher than an organization we interacted with 10 days ago.</p><h2 id="Getting-the-Data"><a href="#Getting-the-Data" class="headerlink" title="Getting the Data"></a>Getting the Data</h2><p>Both of these solutions require populating our database with information regarding how recently we’ve interacted with particular entities. Fortunately, all of this can be done during the data ingestion phase when records are loaded in Elasticsearch. The only requirement is making sure to keep the information in Elasticsearch up to date with our PostgreSQL database</p><p>The one challenge with this solution is that it requires augmenting our documents with special data regarding total interactions and recency of interactions.  The huge advantage, however, is that it works seamlessly. We don’t require there to be any special configuration when we begin working with a new partner.</p><h2 id="Function-Score-Queries"><a href="#Function-Score-Queries" class="headerlink" title="Function Score Queries"></a>Function Score Queries</h2><p>Elasticsearch provides <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl-function-score-query.html" target="_blank" rel="noopener">function score queries</a> that allow you to modify Elasticsearch’s calculated score. Their documentation provides a really simple example explaining boost the relevancy of a document that has many <code>likes</code>:</p><figure class="highlight json"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br></pre></td><td class="code"><pre><span class="line">GET /_search</span><br><span class="line">&#123;</span><br><span class="line">    <span class="attr">"query"</span>: &#123;</span><br><span class="line">        <span class="attr">"function_score"</span>: &#123;</span><br><span class="line">            <span class="attr">"field_value_factor"</span>: &#123;</span><br><span class="line">                <span class="attr">"field"</span>: <span class="string">"likes"</span>,</span><br><span class="line">                <span class="attr">"factor"</span>: <span class="number">1.2</span>,</span><br><span class="line">                <span class="attr">"modifier"</span>: <span class="string">"sqrt"</span>,</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>This code says that every document’s score will be <code>sqrt(1.2 * doc[&#39;likes&#39;].value)</code></p><p>We used a particular kind of Function Score Query known as a <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/query-dsl-function-score-query.html#function-script-score" target="_blank" rel="noopener">Script Score</a>.</p><p>We wanted our ultimate score to be a function of three values: the score returned from elasticsearch, the days since our last interaction, and our total interactions. Initially, our function looked something like this</p><p><code>newscore</code> = _score <em> doc[‘interactions’] </em></p><p>We found that Elasticsearch’s relevancy score was being overwhelmed by parters with a high number of interactions, so much so that given a search term of <code>nick drane</code>, we might return <code>nick smith</code>, simply because we’ve worked with nick smith more.</p><p>We needed to place an upperbound of the potential contribution of the total interactions. After initially looking at <a href="https://en.wikipedia.org/wiki/Logistic_function" target="_blank" rel="noopener">logistic functions</a> and other overly complicated strategies, we settled on a simple step function. If the number of interactions is greater than 25, then we multiply Elasticsearch’s relevancy score by 2, otherwise we multiply it 1.</p><p>The frequency contribution’s function was similarly simple.</p><p>(could be some density/decay function that looks at density of interactions over time)</p><h2 id="challenges"><a href="#challenges" class="headerlink" title="challenges"></a>challenges</h2><p>Keeping the extra attributes up to date</p><p>We did try to combine two guassian curves but couldn’t get it working</p><p>Sometimes was built using a <code>query_string</code> query against the <code>_all</code> field. Sometimes the returned results were good, but most of the time they fell short.  Their problem was simple: how do we ensure that the</p><p>health care<br>select based on recent claim<br>select based on scheduled appointment<br>select based on upcoming event<br>specify table to search</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;A client approached me with a puzzling problem:&lt;/p&gt;
&lt;p&gt;At Fraight, we have an omnisearch interface backed by an Elasticsearch datastore. The interface allows users yo type a freetext query and get a list of database records sorted by relevancy. At it’s core, this is a simple problem: if the user types in &lt;code&gt;Joe&lt;/code&gt;, return all people whose name contains the word &lt;code&gt;Joe&lt;/code&gt;. And indeed, returning all the &lt;code&gt;Joe&amp;#39;s&lt;/code&gt; in the system is trivial; the problem is that we worked hundreds, possibly even thousands of &lt;code&gt;Joes&lt;/code&gt;. How do we identify the particular &lt;code&gt;Joe&lt;/code&gt; that we care about?&lt;/p&gt;
    
    </summary>
    
      <category term="Elasticsearch" scheme="http://nickdrane.com/categories/Elasticsearch/"/>
    
    
  </entry>
  
  <entry>
    <title>Write Your Own React-Redux Connect</title>
    <link href="http://nickdrane.com/write-your-own-redux-connect/"/>
    <id>http://nickdrane.com/write-your-own-redux-connect/</id>
    <published>2017-09-29T18:14:18.000Z</published>
    <updated>2018-09-30T20:53:07.513Z</updated>
    
    <content type="html"><![CDATA[<p><em>My inspiration for this blog post came from <a href="https://www.youtube.com/watch?v=VJ38wSFbM3A" target="_blank" rel="noopener">this video</a> where Dan Abramov walks through the source code to react-redux</em></p><p>As frontend web developers, it’s not uncommon that we follow well-specified patterns - often blindly. The frontend landscape is changing rapidly, and sometimes there isn’t time to investigate why we use a specific pattern; we just know we should.</p><p>One widely used pattern in <a href="https://github.com/reactjs/react-redux" target="_blank" rel="noopener">react-redux</a> applications looks like this</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">connect(mapStateToProps, mapDispatchToProps)(MyComponent)</span><br></pre></td></tr></table></figure><p>I’ll assume you know how to implement this pattern, but why do we use it and how does it work under the hood?</p><a id="more"></a><h1 id="Why-Do-we-Need-React-Redux"><a href="#Why-Do-we-Need-React-Redux" class="headerlink" title="Why Do we Need React-Redux?"></a>Why Do we Need React-Redux?</h1><p>React and Redux are two completely independent tools that have nothing to do with each other. React is a tool for creating user interfaces in the browser. Redux is a tool for managing state. Either tool can be used without the other. We often use them together because they both solve separate but very important and closely related problems. The purpose of react-redux is to get these two tools to talk.</p><p>But first, what would we do without react-redux? How would React and Redux talk?</p><h1 id="How-to-Integrate-React-and-Redux-Without-react-redux"><a href="#How-to-Integrate-React-and-Redux-Without-react-redux" class="headerlink" title="How to Integrate React and Redux Without react-redux"></a>How to Integrate React and Redux Without react-redux</h1><p>More precisely, how do we ensure that a React component re-renders when the Redux store changes? The answer lies in Redux’s <a href="http://redux.js.org/docs/api/Store.html#subscribe" target="_blank" rel="noopener">subscribe</a> API.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> store <span class="keyword">from</span> <span class="string">'./store'</span></span><br><span class="line"><span class="keyword">import</span> &#123; Component &#125; <span class="keyword">from</span> <span class="string">'react'</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">MyComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">  <span class="keyword">constructor</span>() &#123;</span><br><span class="line">    <span class="keyword">super</span>();</span><br><span class="line">    <span class="comment">// One solution is to make each component</span></span><br><span class="line">    <span class="comment">// store the entirety of the redux state.</span></span><br><span class="line">    <span class="keyword">this</span>.state = &#123; <span class="attr">storeState</span>: store.getState() &#125;;</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  componentDidMount() &#123;</span><br><span class="line">    <span class="comment">// Callbacks passed to store.subscribe will be</span></span><br><span class="line">    <span class="comment">// invoked every time the store's state changes.</span></span><br><span class="line">    <span class="comment">// Our callback can get the state of the</span></span><br><span class="line">    <span class="comment">// store and add it to the component's local state.</span></span><br><span class="line">    <span class="keyword">this</span>.unsubscribe = store.subscribe(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">      <span class="keyword">this</span>.setState(&#123; <span class="attr">storeState</span>: store.getState() &#125;);</span><br><span class="line">    &#125;);</span><br><span class="line">  &#125;</span><br><span class="line"></span><br><span class="line">  <span class="comment">// We need to make sure that we don't accidentally</span></span><br><span class="line">  <span class="comment">// subscribe to the store multiple times in the case</span></span><br><span class="line">  <span class="comment">// where a component mounts, unmounts, and then mounts a second time.</span></span><br><span class="line">  <span class="comment">// Fortunately, Redux makes this easy by returning</span></span><br><span class="line">  <span class="comment">// an unsubscribe function when store.subscribe is invoked.</span></span><br><span class="line">  componentWillUnmount() &#123;</span><br><span class="line">    <span class="keyword">this</span>.unsubscribe();</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>If we insert the above boilerplate into every one of our React component’s, then every component could have access to the store and would be informed through a subscription the moment the store’s state changes. This configuration has three  flaws.</p><ol><li>The boilerplate of subscribing and unsubscribing to the store is highly error prone and unnecessarily verbose.</li><li>All of our React component’s are dependent upon knowledge of the Redux store. This is a complete failure of <a href="https://en.wikipedia.org/wiki/Separation_of_concerns" target="_blank" rel="noopener">separation of concerns</a>.</li><li>Every component is dependent upon the entirety of the store’s state tree. This means that whenever an action is dispatched, <code>setState</code> is called on every mounted component, causing each one to re-render, regardless of whether its render function depends on the store state that changed. Woah! Let that sink in for a moment.</li></ol><p>Let’s write a rudimentary implementation of connect that resolves the first problem.</p><h1 id="Understanding-The-Syntax-of-Connect"><a href="#Understanding-The-Syntax-of-Connect" class="headerlink" title="Understanding The Syntax of Connect"></a>Understanding The Syntax of Connect</h1><p>Typically, we invoke <code>connect</code> like this:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">connect(mapStateToProps, mapDispatchToProps)(WrappedComponent);</span><br></pre></td></tr></table></figure><p><code>connect</code> takes in two functions as arguments and returns a function. Yes, you heard me, <code>connect</code> returns a function, not a component. Suppose I invoke <code>connect</code> and neglect to pass in a component.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">const</span> connectFunc = connect(mapStateToProps, mapDispatchToProps);</span><br><span class="line"><span class="keyword">const</span> connctedComponent = connectFunc(WrappedComponent);</span><br></pre></td></tr></table></figure><p> <code>connect</code> will return to me a function. It’s that function that takes in my component (<code>connect</code> is implemented this way as opposed to simply taking in 3 arguments to support decorator syntax. The Dan Abramov video I linked above explains this.)</p><p>Thus, the very first few lines of <code>connect</code> must look like this:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">connect</span>(<span class="params">mapStateToProps, mapDispatchToProps</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line"></span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="Higher-Order-Components"><a href="#Higher-Order-Components" class="headerlink" title="Higher Order Components"></a>Higher Order Components</h1><p>And what does the function we returned above do? This function is implemented as a <a href="https://reactjs.org/docs/higher-order-components.html" target="_blank" rel="noopener">higher order component</a> (HOC). A HOC is a function that takes in a component as a parameter and returns a new component. The new component is generally a modified or augmented version of the original component.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">connect</span>(<span class="params">mapStateToProps, mapDispatchToProps</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">    <span class="comment">// We are returning a brand new component.</span></span><br><span class="line">    <span class="comment">// Note that this new component does</span></span><br><span class="line">    <span class="comment">// not inherit from WrappedComponent.</span></span><br><span class="line">    <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">WrapperComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">      <span class="comment">// All we are doing is returning a new component</span></span><br><span class="line">      <span class="comment">// that renders our original component.</span></span><br><span class="line">      render() &#123;</span><br><span class="line">        <span class="comment">// Notice that we need to pass WrappedComponent</span></span><br><span class="line">        <span class="comment">// WrapperComponent's props.</span></span><br><span class="line">        <span class="comment">// If we didn't do this, then WrappedComponent</span></span><br><span class="line">        <span class="comment">// would never have access to any props.</span></span><br><span class="line">        <span class="keyword">return</span> &lt;WrappedComponent &#123;...this.props&#125; /&gt;;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>If we were to run the above <code>connect</code> function on a component, the connected component would behave identically to original component. Furthermore, we could nest <code>connect</code> as many times as we want</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">connect(<span class="literal">null</span>, <span class="literal">null</span>)(connect(<span class="literal">null</span>, <span class="literal">null</span>)(App))</span><br></pre></td></tr></table></figure><p>and still never distort the behavior of the original component. Our current implementation is effectively <a href="https://stackoverflow.com/questions/1077412/what-is-an-idempotent-operation" target="_blank" rel="noopener">idempotent</a>.</p><h1 id="Eliminating-Boilerplate"><a href="#Eliminating-Boilerplate" class="headerlink" title="Eliminating Boilerplate"></a>Eliminating Boilerplate</h1><p>Our next step is to eliminate some of the boilerplate code. We don’t want to have to subscribe to the store every time we create a new component, so let’s have our new <code>connect</code> function do it instead.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">connect</span>(<span class="params">mapStateToProps, mapDispatchToProps</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">WrapperComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">      <span class="keyword">constructor</span>() &#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="keyword">this</span>.state = &#123; <span class="attr">storeState</span>: store.getState() &#125;;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentDidMount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe = store.subscribe(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">          <span class="keyword">this</span>.setState(&#123; <span class="attr">storeState</span>: store.getState() &#125;);</span><br><span class="line">        &#125;);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentWillUnmount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      render() &#123;</span><br><span class="line">        <span class="comment">// Since the whole point of this HOC is to get WrappedComponent</span></span><br><span class="line">        <span class="comment">// access to the store, we need to pass that state down as props.</span></span><br><span class="line">        <span class="keyword">const</span> storeState = <span class="keyword">this</span>.state.storeState;</span><br><span class="line">        <span class="keyword">return</span> &lt;WrappedComponent &#123;...this.props&#125; &#123;...storeState&#125; /&gt;;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>We just made huge progress! Now, whenever we invoke</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br></pre></td><td class="code"><pre><span class="line">connect(<span class="literal">null</span>, <span class="literal">null</span>)(MyComponent)</span><br></pre></td></tr></table></figure><p>we get a component that is subscribed to state changes on the store, and this state will be passed down to our component as props.</p><h1 id="Implementing-Support-for-mapStateToProps"><a href="#Implementing-Support-for-mapStateToProps" class="headerlink" title="Implementing Support for mapStateToProps"></a>Implementing Support for mapStateToProps</h1><p>Our connected components still all depend on the entirety of the store’s state tree. Look up above, the entire state is passed down as props to every connected component. To reiterate, this means that if any piece of the store’s state is updated, our component will re-render.</p><p>This is where <code>mapStateToProps</code> comes to the rescue. <code>mapStateToProps</code> takes as its argument the store’s state, and it allows us to return the particular pieces of the store’s state that a component depends on. It then passes that state as props to our component instead.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">connect</span>(<span class="params">mapStateToProps, mapDispatchToProps</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">WrapperComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">      <span class="keyword">constructor</span>() &#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="keyword">this</span>.state = &#123; <span class="attr">storeState</span>: store.getState() &#125;;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentDidMount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe = store.subscribe(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">          <span class="keyword">this</span>.setState(&#123; <span class="attr">storeState</span>: store.getState() &#125;);</span><br><span class="line">        &#125;);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentWillUnmount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      render() &#123;</span><br><span class="line">        <span class="comment">// Now, instead of passing down all of the store state,</span></span><br><span class="line">        <span class="comment">// we only pass down the subset of state return from</span></span><br><span class="line">        <span class="comment">// mapStateToProps</span></span><br><span class="line">        <span class="keyword">const</span> storeProps = mapStateToProps(<span class="keyword">this</span>.state.storeState);</span><br><span class="line">        <span class="keyword">return</span> &lt;WrappedComponent &#123;...this.props&#125; &#123;...storeProps&#125; /&gt;;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>All we did was insert a call to <code>mapStateToProps</code>, allowing us to make each connected component dependent upon only the state it cares about, as defined by the return value of <code>mapStateToProps</code>. <code>mapStateToProps</code> is a wonderful form of explicit documentation, clearly stating the slices of the state tree each component depends on. Unfortunately, our change does not fix the efficiency problems noted above. More on that below.</p><h1 id="mapStateToProps-and-ownProps"><a href="#mapStateToProps-and-ownProps" class="headerlink" title="mapStateToProps and ownProps"></a>mapStateToProps and ownProps</h1><p>An astute reader might note that <code>mapStateToProps</code> actually takes two arguments: the first is a copy of the store’s state, and the second are the props that are originally passed down to <code>WrapperComponent</code>. <code>react-redux</code> does not pass these down to the wrapped component by default as we do in the example immediately above. Let’s modify our implementation to mirror <code>react-redux</code>.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">connect</span>(<span class="params">mapStateToProps, mapDispatchToProps</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">WrapperComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">      <span class="keyword">constructor</span>() &#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="keyword">this</span>.state = &#123; <span class="attr">storeState</span>: store.getState() &#125;;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentDidMount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe = store.subscribe(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">          <span class="keyword">this</span>.setState(&#123; <span class="attr">storeState</span>: store.getState() &#125;);</span><br><span class="line">        &#125;);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentWillUnmount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      render() &#123;</span><br><span class="line">        <span class="keyword">const</span> newProps = mapStateToProps(<span class="keyword">this</span>.state.storeState, <span class="keyword">this</span>.props);</span><br><span class="line">        <span class="keyword">return</span> &lt;WrappedComponent &#123;...newProps&#125; /&gt;;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>Now the implementer of <code>mapStateToProps</code> can choose which of <code>WrapperComponent</code>‘s props it would like to keep and which it would like to disregard.</p><h1 id="What’s-the-Point-of-mapDispatchToProps"><a href="#What’s-the-Point-of-mapDispatchToProps" class="headerlink" title="What’s the Point of mapDispatchToProps?"></a>What’s the Point of mapDispatchToProps?</h1><p><code>mapDispatchToProps</code> is designed to eliminate React’s dependency upon Redux. If we were to use the above implementation of <code>connect</code>, every component that dispatch’s an action must import <code>store.dispatch</code>, and the implementation would look like this:</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">import</span> store <span class="keyword">from</span> <span class="string">"./store"</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; Component &#125; <span class="keyword">from</span> <span class="string">"react"</span>;</span><br><span class="line"><span class="keyword">import</span> &#123; updateThing &#125; <span class="keyword">from</span> <span class="string">"./store/actions"</span>;</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">ExampleComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">  handleChange(e) &#123;</span><br><span class="line">    store.dispatch(updateThing(e.target));</span><br><span class="line">  &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>The above component ‘knows’ that it is part of a Redux application because it is explicitly referencing the store to dispatch actions. But we should always try to minimize the interaction of different pieces of architecture, esspecially when they have no need to interact. Ultimately, React components should not been intertwined with Redux code!</p><h2 id="Implementing-Support-for-mapDispatchToProps"><a href="#Implementing-Support-for-mapDispatchToProps" class="headerlink" title="Implementing Support for mapDispatchToProps"></a>Implementing Support for mapDispatchToProps</h2><p><code>connect</code> resolves this problem for us by injecting the <code>store.dispatch</code> dependency into <code>mapDispatchToProps</code>, allowing us to explicitly define functions that dispatch actions without requiring that our <a href="https://medium.com/@dan_abramov/smart-and-dumb-components-7ca2f9a7c7d0" target="_blank" rel="noopener">presentation components</a> have a dependency on the store. Just as the return value of <code>mapStateToProps</code> is passed down to <code>WrappedComponent</code>, the return value of <code>mapDispatchToProps</code> will be passed down as well.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br></pre></td><td class="code"><pre><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">connect</span>(<span class="params">mapStateToProps, mapDispatchToProps</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">WrapperComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">      <span class="keyword">constructor</span>() &#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="keyword">this</span>.state = &#123; <span class="attr">storeState</span>: store.getState() &#125;;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentDidMount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe = store.subscribe(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">          <span class="keyword">this</span>.setState(&#123; <span class="attr">storeState</span>: store.getState() &#125;);</span><br><span class="line">        &#125;);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentWillUnmount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      render() &#123;</span><br><span class="line">        <span class="comment">// Now we merge the results from mapStateToProps</span></span><br><span class="line">        <span class="comment">// and mapDispatchToProps and pass everything down</span></span><br><span class="line">        <span class="keyword">const</span> newProps = <span class="built_in">Object</span>.assign(</span><br><span class="line">          &#123;&#125;,</span><br><span class="line">          mapStateToProps(<span class="keyword">this</span>.state.storeState, <span class="keyword">this</span>.props),</span><br><span class="line">          <span class="comment">// If you aren't intimately familiar with the this keyword,</span></span><br><span class="line">          <span class="comment">// it's okay if you don't understand why we use bind here</span></span><br><span class="line">          mapDispatchToProps(store.dispatch.bind(<span class="keyword">this</span>))</span><br><span class="line">        );</span><br><span class="line">        <span class="keyword">return</span> &lt;WrappedComponent &#123;...newProps&#125; /&gt;;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h1 id="More-Efficiency-Issues-Hello-shouldComponentUpdate"><a href="#More-Efficiency-Issues-Hello-shouldComponentUpdate" class="headerlink" title="More Efficiency Issues - Hello shouldComponentUpdate"></a>More Efficiency Issues - Hello shouldComponentUpdate</h1><p>We never actually fixed any of the performance issues noted above. The crux of the problem is that every time the store updates, <code>WrapperComponent</code> re-renders (because of its Redux store subscription that calls <code>setState</code>) and that means <code>WrappedComponent</code> re-renders. This <a href="/leveraging-immutability-in-react">re-rendering</a> happens despite the fact that <code>WrappedComponent</code>‘s props might be unchanged between two invocations of <code>setState</code>. In fact, this scenario is highly probable and will occur whenever a piece of state in the store changes that your component does not depend on (aka, a piece of store state not returned from from <code>mapStateToProps</code>).</p><p>React has a handy lifecycle method called <a href="https://reactjs.org/docs/react-component.html#shouldcomponentupdate" target="_blank" rel="noopener"><code>shouldComponentUpdate</code></a> that allows us to return a boolean that indicates whether a component should re-render. In essence, if we implement this method on <code>WrapperComponent</code> and it returns <code>false</code>, then React will not re-render <code>WrapperComponent</code>. And it follows that <code>WrappedComponent</code> won’t re-render either.</p><p>So, in the above scenario, when <code>WrapperComponent</code> calls <code>setState</code>, React first calls the <code>shouldComponentUpdate</code> method to see if a re-render should actually happen. Let’s implement it below.</p><figure class="highlight js"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br><span class="line">32</span><br><span class="line">33</span><br><span class="line">34</span><br><span class="line">35</span><br><span class="line">36</span><br><span class="line">37</span><br><span class="line">38</span><br><span class="line">39</span><br><span class="line">40</span><br><span class="line">41</span><br><span class="line">42</span><br><span class="line">43</span><br><span class="line">44</span><br><span class="line">45</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// Just a simple shallow equality function</span></span><br><span class="line"><span class="keyword">import</span> shallowEqual <span class="keyword">from</span> <span class="string">"shallow-equal/objects"</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">function</span> <span class="title">connect</span>(<span class="params">mapStateToProps, mapDispatchToProps</span>) </span>&#123;</span><br><span class="line">  <span class="keyword">return</span> <span class="function"><span class="keyword">function</span>(<span class="params">WrappedComponent</span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> <span class="class"><span class="keyword">class</span> <span class="title">WrapperComponent</span> <span class="keyword">extends</span> <span class="title">Component</span> </span>&#123;</span><br><span class="line">      <span class="keyword">constructor</span>() &#123;</span><br><span class="line">        <span class="keyword">super</span>();</span><br><span class="line">        <span class="keyword">this</span>.state = &#123; <span class="attr">storeState</span>: store.getState() &#125;;</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      shouldComponentUpdate() &#123;</span><br><span class="line">        <span class="comment">// If the props to WrapperComponent do not change</span></span><br><span class="line">        <span class="comment">// between setState calls, then we don't need to re-render.</span></span><br><span class="line">        <span class="comment">// On the previous re-render, we cached the results of</span></span><br><span class="line">        <span class="comment">// mapStateToProps. That's what this.oldProps is.</span></span><br><span class="line">        <span class="keyword">const</span> newProps = mapStateToProps(<span class="keyword">this</span>.state.storeState, <span class="keyword">this</span>.props);</span><br><span class="line">        <span class="keyword">return</span> !shallowEqual(newProps, <span class="keyword">this</span>.oldProps);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentDidMount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe = store.subscribe(<span class="function"><span class="params">()</span> =&gt;</span> &#123;</span><br><span class="line">          <span class="keyword">this</span>.setState(&#123; <span class="attr">storeState</span>: store.getState() &#125;);</span><br><span class="line">        &#125;);</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      componentWillUnmount() &#123;</span><br><span class="line">        <span class="keyword">this</span>.unsubscribe();</span><br><span class="line">      &#125;</span><br><span class="line"></span><br><span class="line">      render() &#123;</span><br><span class="line">        <span class="comment">// We need to hang onto the previous result of</span></span><br><span class="line">        <span class="comment">// mapStateToProps to use the next time</span></span><br><span class="line">        <span class="comment">// shouldComponentUpdate runs</span></span><br><span class="line">        <span class="keyword">this</span>.oldProps = mapStateToProps(<span class="keyword">this</span>.state.storeState, <span class="keyword">this</span>.props)</span><br><span class="line">        <span class="keyword">const</span> newProps = <span class="built_in">Object</span>.assign(</span><br><span class="line">          &#123;&#125;,</span><br><span class="line">          <span class="keyword">this</span>.oldProps,</span><br><span class="line">          mapDispatchToProps(store.dispatch.bind(<span class="keyword">this</span>))</span><br><span class="line">        );</span><br><span class="line">        <span class="keyword">return</span> &lt;WrappedComponent &#123;...newProps&#125; /&gt;;</span><br><span class="line">      &#125;</span><br><span class="line">    &#125;;</span><br><span class="line">  &#125;;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>I’ve created a demo <a href="https://codesandbox.io/s/o43p70k66" target="_blank" rel="noopener">here</a>. Open the console and prove to yourself that <code>shouldComponentUpdate</code> is doing its job.</p><p><em>I should note that this is not exactly what react-redux does because of edge cases, but the concept is still the same.</em></p><p>Now our wrapper and wrapped components will only re-render when the props returned from <code>mapStateToProps</code> change! This is a huge performance gain. This implementation of <code>connect</code> explains why adherence to <a href="http://redux.js.org/docs/faq/ReactRedux.html#react-not-rerendering" target="_blank" rel="noopener">immutability is so important</a> in redux’s reducers. If you fail to respect immutability, the shallow comparison in the <code>shouldComponentUpdate</code> in <code>WrapperComponent</code> will likely return <code>false</code>, causing your connected component to not re-render when it should.</p><h1 id="Wrapping-up"><a href="#Wrapping-up" class="headerlink" title="Wrapping up"></a>Wrapping up</h1><p>React-redux’s <code>connect</code> method is remarkably simple and only performs a handful of operations.</p><ol><li>It manages our component’s subscription to the store so that our component can update when the store updates.</li><li>It allows us to explicitly define the slice of state our component is dependent upon using <code>mapStateToProps</code>.</li><li>It gives our component access to <code>store.dispatch</code> without requiring a direct dependency on the store.</li><li>It defines <code>shouldComponentUpdate</code>, ensuring that our components only re-render when the store state they depend on changes.</li></ol><p>I hope you found this article helpful. Please feel free to email me and reach out if you have questions. I put a <a href="https://gist.github.com/nadrane/5221c64c421efe421bda9fdaab167dc2" target="_blank" rel="noopener">gist</a> online containing the same code as the demo.</p>]]></content>
    
    <summary type="html">
    
      &lt;p&gt;&lt;em&gt;My inspiration for this blog post came from &lt;a href=&quot;https://www.youtube.com/watch?v=VJ38wSFbM3A&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;this video&lt;/a&gt; where Dan Abramov walks through the source code to react-redux&lt;/em&gt;&lt;/p&gt;
&lt;p&gt;As frontend web developers, it’s not uncommon that we follow well-specified patterns - often blindly. The frontend landscape is changing rapidly, and sometimes there isn’t time to investigate why we use a specific pattern; we just know we should.&lt;/p&gt;
&lt;p&gt;One widely used pattern in &lt;a href=&quot;https://github.com/reactjs/react-redux&quot; target=&quot;_blank&quot; rel=&quot;noopener&quot;&gt;react-redux&lt;/a&gt; applications looks like this&lt;/p&gt;
&lt;figure class=&quot;highlight js&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;gutter&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;1&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;td class=&quot;code&quot;&gt;&lt;pre&gt;&lt;span class=&quot;line&quot;&gt;connect(mapStateToProps, mapDispatchToProps)(MyComponent)&lt;/span&gt;&lt;br&gt;&lt;/pre&gt;&lt;/td&gt;&lt;/tr&gt;&lt;/table&gt;&lt;/figure&gt;
&lt;p&gt;I’ll assume you know how to implement this pattern, but why do we use it and how does it work under the hood?&lt;/p&gt;
    
    </summary>
    
      <category term="React" scheme="http://nickdrane.com/categories/React/"/>
    
      <category term="Build Your Own" scheme="http://nickdrane.com/categories/Build-Your-Own/"/>
    
      <category term="Redux" scheme="http://nickdrane.com/categories/Redux/"/>
    
    
  </entry>
  
</feed>
